const __modules = {};
__modules['format']=(()=>{
const escapeHTML=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const formatARS=c=>new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',minimumFractionDigits:c%100?2:0,maximumFractionDigits:2}).format(Number(c||0)/100);
const duration=b=>b*15<60?`${b*15} min`:`${Math.floor(b*15/60)} h${b*15%60?' '+b*15%60+' min':''}`;
const dateLabel=value=>new Date(value).toLocaleString('es-AR',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'});
const dateInput=value=>{const d=new Date(value);return new Date(d-d.getTimezoneOffset()*60000).toISOString().slice(0,16);};
const normal=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
function preview(priceArs,blocks){if(![1000,2500,5000].includes(priceArs)||!Number.isInteger(blocks)||blocks<1||blocks>32)throw new Error('Elegí una tarifa y de 1 a 32 bloques.');const grossCents=priceArs*blocks*100,commissionCents=grossCents/20;return {grossCents,commissionCents,sellerBeforeProcessorCents:grossCents-commissionCents,minutes:blocks*15};}
const statusLabel=s=>({REQUESTED:'Esperando aceptación',ACCEPTED:'Aceptado',IN_PROGRESS:'En curso',COMPLETED_BY_PROVIDER:'Confirmación pendiente',COMPLETED:'Completado',CANCELLED:'Cancelado',DISPUTED:'En revisión',UNPAID:'Sin pagar',CHECKOUT_CREATED:'Checkout disponible',PENDING:'Pago pendiente',APPROVED:'Pago aprobado',REJECTED:'Pago rechazado',REFUND_PENDING:'Devolución en proceso',REFUNDED:'Devuelto',PARTIALLY_REFUNDED:'Devolución parcial',CHARGED_BACK:'Contracargo',MEDIATION:'Mediación de pago',PAYMENT_REVIEW:'Pago en revisión',CONNECTED:'Vinculada',PAUSED:'Pausada',OPEN:'Abierto',CLOSED:'Cerrado',PROCESSING:'Procesando',SUBMITTED:'Confirmando devolución',READY:'Preparado',UNKNOWN:'Requiere conciliación'})[s]||String(s||'—');
function csv(rows){return '\uFEFF'+rows.map(row=>row.map(v=>{let s=String(v??'');if(/^[\s]*[=+\-@]/.test(s))s="'"+s;return '"'+s.replaceAll('"','""')+'"';}).join(',')).join('\r\n');}
function calendarICS(events,demo=false){const stamp=n=>new Date(n).toISOString().replace(/[-:]/g,'').replace(/\.\d{3}Z$/,'Z');const esc=s=>String(s||'').replaceAll('\\','\\\\').replace(/\r\n|\r|\n/g,'\\n').replaceAll(';','\\;').replaceAll(',','\\,');const lines=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//INTERA//Agenda v5//ES','CALSCALE:GREGORIAN'];for(const e of events)lines.push('BEGIN:VEVENT','UID:'+esc(e.id)+'@intera','DTSTAMP:'+stamp(Date.now()),'DTSTART:'+stamp(e.scheduled_at),'DTEND:'+stamp(e.end_at),'SUMMARY:'+esc((demo?'DEMO · ':'')+e.title),'DESCRIPTION:'+esc('Servicio con '+e.seller_name+'. Exportación personal; no envía invitaciones.'),'END:VEVENT');lines.push('END:VCALENDAR');return lines.map(line=>{let out='',part='',bytes=0;for(const c of line){const b=new TextEncoder().encode(c).length;if(bytes+b>73){out+=part+'\r\n ';part='';bytes=1;}part+=c;bytes+=b;}return out+part;}).join('\r\n')+'\r\n';}
function download(name,content,type='text/plain;charset=utf-8'){const url=URL.createObjectURL(new Blob([content],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);}
function routeFromHash(fragment){const raw=String(fragment||'#/').replace(/^#/,'');if(!raw.startsWith('/')||raw.startsWith('//'))return {route:'/',params:new URLSearchParams()};const at=raw.indexOf('?');const path=at<0?raw:raw.slice(0,at);return {route:path.replace(/\/$/,'')||'/',params:new URLSearchParams(at<0?'':raw.slice(at+1))};}
// A cryptographic idempotency key is required; never fall back to Math.random.
function requestKey(source=globalThis.crypto){if(typeof source?.randomUUID==='function')return source.randomUUID();if(typeof source?.getRandomValues!=='function')throw new Error('El navegador no ofrece aleatoriedad criptográfica. Usá un navegador actualizado en HTTPS.');return Array.from(source.getRandomValues(new Uint8Array(16)),n=>n.toString(16).padStart(2,'0')).join('');}

return {escapeHTML,formatARS,duration,dateLabel,dateInput,normal,preview,statusLabel,csv,calendarICS,download,routeFromHash,requestKey};})();
__modules['icons']=(()=>{
const shapes={home:'<path d="m3 10 9-7 9 7v10H3Z"/><path d="M9 20v-7h6v7"/>',search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 6v6l4 2"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',plus:'<path d="M12 4v16M4 12h16"/>',heart:'<path d="M20.8 4.7a5.5 5.5 0 0 0-7.8 0L12 5.8l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 21l8.8-8.5a5.5 5.5 0 0 0 0-7.8Z"/>',swap:'<path d="M3 7h18l-4-4M21 17H3l4 4"/>',wallet:'<path d="M3 6h18v15H3zM3 6l15-3v3m-1 7h4m-4 4h4"/>',calendar:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M7 3v5m10-5v5M3 11h18m-13 4h2m4 0h2"/>',user:'<circle cx="12" cy="8" r="4"/><path d="M4 21v-2a8 8 0 0 1 16 0v2"/>',users:'<circle cx="9" cy="8" r="3"/><path d="M3 20v-2a6 6 0 0 1 12 0v2m2-15a3 3 0 0 1 0 6m2 4a5 5 0 0 1 2 5"/>',check:'<path d="m5 12 4 4L19 6"/>',close:'<path d="m6 6 12 12M18 6 6 18"/>',bell:'<path d="M5 9a7 7 0 0 1 14 0v6l2 3H3l2-3zm5 12h4"/>',shield:'<path d="m12 2 9 4v6c0 5-9 10-9 10S3 17 3 12V6Z"/><path d="m8 11 3 3 5-6"/>',chevron:'<path d="m9 5 7 7-7 7"/>',pin:'<path d="M20 10c0 6-8 12-8 12S4 16 4 10a8 8 0 1 1 16 0Z"/><circle cx="12" cy="10" r="2"/>',chat:'<path d="M21 4H3v14h5l4 4 4-4h5ZM7 9h10M7 13h7"/>',send:'<path d="m22 2-7 20-4-9L2 9Z M11 13 22 2"/>',star:'<path d="m12 3 3 6 7 1-5 5 1 7-6-3-6 3 1-7-5-5 7-1Z"/>',sun:'<circle cx="12" cy="12" r="4"/><path d="M12 1v3m0 16v3M1 12h3m16 0h3M4 4l2 2m12 12 2 2M4 20l2-2M18 6l2-2"/>',moon:'<path d="M21 13A9 9 0 1 1 11 3a7 7 0 0 0 10 10Z"/>',download:'<path d="M12 3v12m-5-5 5 5 5-5M3 16v5h18v-5"/>',link:'<path d="m10 14 4-4m-6 1-3 3a4 4 0 0 0 6 6l3-3m-4-10 3-3a4 4 0 0 1 6 6l-3 3"/>',help:'<circle cx="12" cy="12" r="9"/><path d="M9 8a3 3 0 0 1 6 0c0 2-3 2-3 5m0 3v1"/>',filter:'<path d="M3 6h18M3 12h18M3 18h18"/><circle cx="8" cy="6" r="2"/><circle cx="16" cy="12" r="2"/><circle cx="9" cy="18" r="2"/>',briefcase:'<rect x="3" y="7" width="18" height="14" rx="2"/><path d="M8 7V3h8v4M3 12l9 3 9-3m-9 0v4"/>',tech:'<rect x="4" y="3" width="16" height="13" rx="2"/><path d="M1 21h22M8 16l-2 5m10-5 2 5"/>',design:'<path d="m3 21 4-1L20 7l-3-3L4 17Zm10-14 4 4M3 21h18"/>',language:'<path d="M3 5h12M9 2v3M5 5c1 5 4 8 9 10M12 5c-1 5-4 9-9 11m11 5 4-12 4 12m-6-4h4"/>',camera:'<path d="M3 7h4l2-3h6l2 3h4v14H3Z"/><circle cx="12" cy="13" r="4"/>',code:'<path d="m7 7-5 5 5 5m10-10 5 5-5 5M14 3l-4 18"/>',list:'<path d="M8 6h13M8 12h13M8 18h13M3 6h1M3 12h1M3 18h1"/>',spark:'<path d="m12 2 3 7 7 3-7 3-3 7-3-7-7-3 7-3Z"/>',logout:'<path d="M9 3H3v18h6m5-15 6 6-6 6m-8-6h14"/>',refresh:'<path d="M21 3v6h-6M3 21v-6h6M5 8a8 8 0 0 1 13-3l3 4M3 15l3 4a8 8 0 0 0 13-3"/>',compare:'<path d="M8 3v18m8-18v18M3 8h10M11 16h10"/>',info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v6m0-11v1"/>'};
const icon=(name,cls='')=>`<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${shapes[name]||shapes.spark}</svg>`;
const categoryIcon=c=>({'Tecnología':'tech','Idiomas':'language','Diseño':'design','Fotografía':'camera','Programación':'code','Organización':'list','Redacción':'design'})[c]||'spark';

return {icon,categoryIcon};})();
__modules['api']=(()=>{
let csrf=null;
class APIError extends Error{constructor(message,code,status){super(message);this.code=code;this.status=status;}}
function setCSRF(value){csrf=value;}
async function api(path,{method='GET',data,key}={}){const h={Accept:'application/json'};if(data!==undefined)h['Content-Type']='application/json';if(csrf)h['X-CSRF-Token']=csrf;if(key)h['Idempotency-Key']=key;let r;try{r=await fetch('/api'+path,{method,headers:h,credentials:'same-origin',cache:'no-store',...(data===undefined?{}:{body:JSON.stringify(data)}),signal:AbortSignal.timeout(25000)});}catch{throw new APIError('No se pudo confirmar la respuesta del servidor. Consultá la orden antes de repetir un pago.','NETWORK',0);}let value;try{value=await r.json();}catch{throw new APIError('El servidor no devolvió una respuesta válida.','SERVER',r.status);}if(!r.ok)throw new APIError(value.error||'No se pudo completar la acción.',value.code,r.status);return value;}

return {APIError,setCSRF,api};})();
__modules['motion']=(()=>{
/** Presentation-only motion. Never imports or mutates the ledger or application state. */
function resolveMotion(preference='auto',systemReduced=false) {
 if(preference==='off')return 'off';
 if(systemReduced||preference==='reduced')return 'reduced';
 return 'full';
}
function counterFrame(from,to,progress) {
 const a=Number.isFinite(from)?Math.max(0,from):0;
 const b=Number.isFinite(to)?Math.max(0,to):0;
 const t=Math.max(0,Math.min(1,Number(progress)||0));
 return t===1?b:a+(b-a)*(1-Math.pow(1-t,3));
}
function createMotionController(getPreference) {
 const doc=document;const media=window.matchMedia?.('(prefers-reduced-motion: reduce)');
 let policy='off';const active=new Set();const counters=new Set();const values=new Map();let observer=null;
 function stop() {
   for(const a of active){try{a.cancel();}catch{}}active.clear();
   for(const c of counters){cancelAnimationFrame(c.frame);c.finish();}counters.clear();
   doc.querySelectorAll('.touch-ripple,.confetti-layer').forEach(el=>el.remove());
 }
 function sync(){policy=resolveMotion(getPreference(),!!media?.matches);doc.documentElement.dataset.motion=policy;if(policy!=='full')stop();return policy;}
 function play(el,frames,options={}) {
   if(policy!=='full'||doc.hidden||!el?.animate)return null;
   const a=el.animate(frames,{duration:260,easing:'cubic-bezier(.2,.8,.2,1)',...options});active.add(a);
   a.finished.then(()=>active.delete(a)).catch(()=>active.delete(a));return a;
 }
 function enter(el,index=0){play(el,[{opacity:.15,transform:'translateY(12px)'},{opacity:1,transform:'translateY(0)'}],{duration:360,delay:Math.min(index,4)*38});}
 function numbers(root){
   root.querySelectorAll('[data-count-to]').forEach(el=>{
     const key=el.dataset.countKey;const to=Number(el.dataset.countTo);if(!Number.isFinite(to))return;
     const from=values.has(key)?values.get(key):0;values.set(key,to);
     if(from===to||policy!=='full'||doc.hidden)return;
     const finish=()=>{el.textContent=to.toLocaleString('es-AR',{maximumFractionDigits:2});};
     const run={frame:0,finish};counters.add(run);let start;
     function tick(ts){
       if(!el.isConnected||policy!=='full'){finish();counters.delete(run);return;}
       if(start===undefined)start=ts;const t=Math.min(1,(ts-start)/600);
       el.textContent=counterFrame(from,to,t).toLocaleString('es-AR',{maximumFractionDigits:2});
       if(t<1)run.frame=requestAnimationFrame(tick);else{finish();counters.delete(run);}
     }
     run.frame=requestAnimationFrame(tick);
   });
 }
 function updateCarousel(track){
   if(!track)return;const id=track.id;const max=track.scrollWidth-track.clientWidth;
   doc.querySelectorAll('[data-action="carousel-prev"],[data-action="carousel-next"]').forEach(btn=>{if(btn.dataset.id!==id)return;btn.disabled=btn.dataset.action==='carousel-prev'?track.scrollLeft<=2:track.scrollLeft>=max-2;});
 }
 function refresh(root,{transition=false}={}) {
   sync();for(const c of counters){cancelAnimationFrame(c.frame);c.finish();}counters.clear();
   for(const a of active){if(!a.effect?.target?.isConnected){a.cancel();active.delete(a);}}
   observer?.disconnect();observer=null;
   if(transition&&policy==='full'){
     let i=0;root.querySelectorAll('[data-enter],.page-title,.flow-track,.action-panel').forEach(el=>{
       const r=el.getBoundingClientRect();if(r.top<window.innerHeight&&r.bottom>0){enter(el,i++);}
       else if('IntersectionObserver' in window){
         if(!observer)observer=new IntersectionObserver(entries=>{for(const e of entries)if(e.isIntersecting){enter(e.target);observer.unobserve(e.target);}},{threshold:.12});
         observer.observe(el);
       }
     });
   }
   numbers(root);
   requestAnimationFrame(()=>root.querySelectorAll('[data-carousel]').forEach(updateCarousel));
 }
 function ripple(event) {
   if(policy!=='full'||event.button!==0||doc.hidden)return;
   const button=event.target.closest('.btn,.icon-btn,.offer-save,.category-pill,.category-chip,.nav-item,.bottom-nav a,.choice-card');
   if(!button||button.disabled)return;
   const r=doc.createElement('span');r.className='touch-ripple';r.setAttribute('aria-hidden','true');
   r.style.left=event.clientX+'px';r.style.top=event.clientY+'px';doc.body.append(r);
   const a=play(r,[{opacity:.25,transform:'translate(-50%,-50%) scale(.1)'},{opacity:0,transform:'translate(-50%,-50%) scale(1)'}],{duration:480});
   if(a)a.finished.then(()=>r.remove()).catch(()=>r.remove());else r.remove();
 }
 function celebrate(){
   if(policy!=='full'||doc.hidden)return;
   doc.querySelectorAll('.confetti-layer').forEach(el=>el.remove());const layer=doc.createElement('div');layer.className='confetti-layer';layer.setAttribute('aria-hidden','true');doc.body.append(layer);
   const jobs=[];
   for(let i=0;i<24;i++){
     const p=doc.createElement('i');p.className='confetti-piece piece-'+i%4;p.style.left=(35+(i*17%30))+'%';p.style.top='50%';layer.append(p);
     const x=(i%2===0?-1:1)*(30+i*6);const y=60+(i*19%160);
     const a=play(p,[{opacity:0,transform:'translate(0,0) rotate(0deg)'},{opacity:1,offset:.12},{opacity:0,transform:`translate(${x}px,${y}px) rotate(${(i%2?1:-1)*250}deg)`}],{duration:1000+(i%4)*130,delay:(i%3)*45,easing:'cubic-bezier(.16,.65,.5,1)'});
     if(a)jobs.push(a.finished.catch(()=>{}));
   }
   Promise.all(jobs).then(()=>layer.remove());
 }
 function feedback(type,selector){
   const el=typeof selector==='string'?doc.querySelector(selector):selector;
   if(type==='favorite'||type==='compare')play(el,[{transform:'scale(1)'},{transform:'scale(1.19)',offset:.45},{transform:'scale(1)'}],{duration:380});
   if(type==='message')play(el,[{opacity:.1,transform:'translateY(8px) scale(.98)'},{opacity:1,transform:'translateY(0) scale(1)'}],{duration:250});
   if(type==='error')play(el,[{transform:'translateX(0)'},{transform:'translateX(-3px)'},{transform:'translateX(3px)'},{transform:'translateX(0)'}],{duration:200});
   if(type==='success'){play(el,[{boxShadow:'0 0 0 0 rgba(0,165,150,.28)'},{boxShadow:'0 0 0 15px rgba(0,165,150,0)'}],{duration:750});celebrate();}
 }
 function dialog(el){play(el,[{opacity:.1,transform:'translateY(14px) scale(.992)'},{opacity:1,transform:'translateY(0) scale(1)'}],{duration:230});}
 function scroll(id,dir){const track=doc.getElementById(id);if(!track)return;track.scrollBy({left:dir*(track.clientWidth*.87),behavior:policy==='full'?'smooth':'instant'});}
 const onScroll=e=>{if(e.target?.matches?.('[data-carousel]'))updateCarousel(e.target);};
 const onKey=e=>{if(e.target?.matches?.('[data-carousel]')&&['ArrowRight','ArrowLeft'].includes(e.key)){e.preventDefault();scroll(e.target.id,e.key==='ArrowRight'?1:-1);}};
 const onVisibility=()=>{if(doc.hidden)stop();};
 doc.addEventListener('pointerdown',ripple,{passive:true});doc.addEventListener('scroll',onScroll,true);doc.addEventListener('keydown',onKey);doc.addEventListener('visibilitychange',onVisibility);media?.addEventListener?.('change',sync);
 sync();
 return {refresh,feedback,dialog,scroll,sync,stop,policy:()=>policy,destroy(){stop();observer?.disconnect();doc.removeEventListener('pointerdown',ripple);doc.removeEventListener('scroll',onScroll,true);doc.removeEventListener('keydown',onKey);doc.removeEventListener('visibilitychange',onVisibility);media?.removeEventListener?.('change',sync);}};
}

return {resolveMotion,counterFrame,createMotionController};})();
__modules['views']=(()=>{
const {escapeHTML:h,formatARS:money,duration,dateLabel,dateInput,preview,statusLabel,normal}=__modules['format'];
const {icon,categoryIcon}=__modules['icons'];
const button=(label,action,id='',cls='secondary',extra='')=>`<button class="btn ${cls}" data-action="${action}" data-id="${h(id)}" ${extra}>${label}</button>`;
const link=(label,route,cls='secondary')=>`<a class="btn ${cls}" href="#${route}">${label}</a>`;
function avatar(p,size=''){const slug=p?.is_demo&&['demo_ana','demo_mateo','demo_lucia','demo_carla','demo_santi'].includes(p.id)?p.id.slice(5):null;return `<span class="avatar ${size}">${slug?`<img src="/assets/photos/portrait-${slug}.webp" alt="" loading="lazy">`:h((p?.name||'INTERA').split(' ').slice(0,2).map(w=>w[0]).join(''))}</span>`;}
const person=(ctx,id)=>ctx.data.people.find(p=>p.id===id)||{id,name:'Persona'};
const photoFor=o=>o.is_demo?`/assets/photos/${o.category==='Tecnología'||o.category==='Programación'?'scene-tech':o.category==='Diseño'||o.category==='Fotografía'||o.category==='Redacción'?'scene-design':o.provider_id==='demo_carla'?'portrait-carla':'portrait-ana'}.webp`:null;
const badge=(status,kind='')=>`<span class="badge ${kind||(['APPROVED','COMPLETED','CONNECTED'].includes(status)?'good':['CANCELLED','REJECTED','CHARGED_BACK'].includes(status)?'bad':['PENDING','REFUND_PENDING','DISPUTED','UNPAID','UNKNOWN'].includes(status)?'warning':'')}">${h(statusLabel(status))}</span>`;
const empty=(title,description,cta='',type='search')=>`<section class="empty">${icon(type)}<h3>${h(title)}</h3><p>${h(description)}</p>${cta}</section>`;
const head=(title,subtitle='',actions='',eyebrow='INTERA · SERVICIOS')=>`<header class="page-head" data-enter><div><div class="eyebrow">${h(eyebrow)}</div><h1>${h(title)}</h1>${subtitle?`<p>${h(subtitle)}</p>`:''}</div>${actions?`<div class="actions">${actions}</div>`:''}</header>`;
function notice(content,type=''){return `<div class="notice ${type}">${icon(type==='success'?'check':'info')}<div>${content}</div></div>`;}
function quoteBox(price,blocks,{seller=false,totalLabel='Total del servicio'}={}){let q;try{q=preview(Number(price),Number(blocks));}catch{return notice('Elegí una tarifa y cantidad de bloques válidas.');}return `<div class="breakdown"><div class="line"><span>${blocks} bloque${blocks===1?'':'s'} × ${money(price*100)}</span><strong>${duration(blocks)}</strong></div><div class="line total"><span>${h(totalLabel)}</span><strong>${money(q.grossCents)}</strong></div><div class="line"><span>Comisión INTERA · 5% del bruto</span><strong>${money(q.commissionCents)}</strong></div><div class="line"><span>Prestador, antes de cargos externos</span><strong>${money(q.sellerBeforeProcessorCents)}</strong></div><p>${seller?'La comisión se descuenta del cobro; no se agrega al total del cliente.':'El 5% está incluido: no se agrega como un recargo al total del servicio.'} Los cargos de Mercado Pago y las retenciones aplicables se descuentan por separado. El neto y la fecha de disponibilidad los informa Mercado Pago.</p></div>`;}
function consentBanner(ctx){if(!ctx.user)return '';if(ctx.user.suspended)return notice('Tu cuenta tiene operaciones restringidas. Conservás acceso al historial, exportación y <a class="text-link" href="#/ayuda">reclamos</a>.','warning');if(!ctx.user.emailVerified)return `<div class="notice warning consent-banner"><div><strong>Verificá tu correo para operar.</strong><br>Te enviamos un enlace. En simulación, se muestra dentro de la aplicación.</div>${button('Reenviar verificación','resend','','secondary small')}</div>`;if(!ctx.session.conditionsAccepted)return `<div class="notice warning consent-banner"><div><strong>Tenemos un nuevo modelo de servicios pagos.</strong><br>Leé y aceptá las condiciones antes de publicar, contratar o cobrar.</div>${button('Leer y aceptar','terms','','secondary small')}</div>`;return '';}
function shell(ctx,content){const route=ctx.route;const navigation=[['/','home','Inicio'],['/explorar','search','Explorar'],['/necesidades','list','Necesidades'],['/ordenes','swap','Mis servicios'],['/agenda','calendar','Agenda'],['/movimientos','wallet','Pagos y cobros']];const pending=ctx.data.bookings.filter(b=>b.status==='REQUESTED'&&b.seller_id===ctx.user?.id||b.status==='COMPLETED_BY_PROVIDER'&&b.buyer_id===ctx.user?.id).length;
 const nav=(p,i,title)=>`<a class="nav-item ${route===p?'active':''}" href="#${p}">${icon(i)}${title}${p==='/ordenes'&&pending?`<span class="count">${pending}</span>`:''}</a>`;
 return `<div class="shell"><aside class="sidebar"><a class="brand" href="#/"><img src="/assets/intera-mark.svg" alt="">intera</a><div class="nav-group-label">TU COMUNIDAD</div><nav aria-label="Principal">${navigation.map(n=>nav(...n)).join('')}</nav><div class="nav-group-label">TU ESPACIO</div>${nav('/prestador','briefcase','Soy prestador')}${nav('/perfil','user','Mi perfil')}${nav('/ayuda','help','Ayuda y reclamos')}<div class="sidebar-bottom"><div class="sidebar-note">${icon('clock')}<strong>15 minutos. Algo resuelto.</strong>Tarifas claras, elegidas por quien ofrece. Vos elegís con quién conectar.</div><a class="nav-item" href="#/condiciones">${icon('shield')}Condiciones del servicio</a></div></aside><div class="workspace"><header class="topbar"><a class="brand mobile-brand" href="#/"><img src="/assets/intera-mark.svg" alt="">intera</a><button class="global-search" data-action="search" aria-label="Buscar en INTERA">${icon('search')}<span>¿Qué necesitás resolver?</span><kbd>⌘ K</kbd></button><div class="topbar-right"><span class="topbar-location">${icon('pin','tiny-icon')} ${h(ctx.user?.neighborhood||'Comunidad CABA')}</span><button class="icon-btn theme-btn" data-action="theme" aria-label="Cambiar tema">${icon(ctx.theme==='dark'?'sun':'moon')}</button><button class="icon-btn" data-action="activity" aria-label="Ver actividad pendiente">${icon('bell')}${pending?`<span class="small">${pending}</span>`:''}</button>${ctx.user?`<button class="account-btn" data-action="account" aria-label="Abrir mi cuenta">${avatar(ctx.user)}<strong>${h(ctx.user.name.split(' ')[0])}</strong></button>`:link('Ingresar','/acceso','small')}</div></header><div class="mode-strip ${ctx.config.mode==='live'?'live':''}"><span class="mode-dot"></span><strong>${ctx.config.mode==='mock'?'SIMULADOR LOCAL · SIN DINERO REAL':ctx.config.mode==='sandbox'?'PRUEBAS MERCADO PAGO · SIN COBROS REALES':'PAGOS CON MERCADO PAGO'}</strong><span class="mode-extra">${ctx.config.mode==='mock'?'Perfiles y fotos de ejemplo':'Cuenta y disponibilidad según el proveedor de pagos'}</span>${ctx.config.mode==='mock'?'<button data-action="demo">Cambiar perfil de prueba</button>':''}</div><main id="main" class="content" tabindex="-1">${consentBanner(ctx)}${content}<footer class="footer"><span>INTERA 5.0 · Bloques de 15 minutos · ARS</span><div class="links"><a href="#/condiciones">Condiciones y privacidad</a><a href="#/ayuda">Reclamos</a><button class="compare-button" data-action="settings">Experiencia visual</button></div></footer></main></div><nav class="bottom-nav" aria-label="Navegación móvil">${[['/','home','Inicio'],['/explorar','search','Explorar'],['/publicar','plus','Ofrecer'],['/ordenes','swap','Servicios'],['/perfil','user','Mi espacio']].map(([p,i,t])=>`<a href="#${p}" class="${route===p?'active':''} ${p==='/publicar'?'central':''}">${icon(i)}${t}</a>`).join('')}</nav>${compareTray(ctx)}</div>`;
}
function offerCard(ctx,o){const saved=ctx.data.favorites.includes(o.id),selected=ctx.compare.includes(o.id),src=photoFor(o);return `<article class="offer-card" data-enter><div class="offer-photo">${src?`<img src="${src}" alt="Imagen ilustrativa de demostración" loading="lazy"><div class="photo-tint"></div>`:icon(categoryIcon(o.category))}<span class="photo-label">${icon('tech')}Remoto · 18+</span><button class="offer-save" data-action="favorite" data-id="${h(o.id)}" aria-label="${saved?'Quitar de':'Agregar a'} guardados: ${h(o.title)}" aria-pressed="${saved}">${icon('heart')}</button></div><div class="offer-content"><a href="#/perfil/${h(o.provider_id)}" class="provider-line">${avatar(person(ctx,o.provider_id))}<span>${h(o.provider_name)}</span></a><h3><a href="#/oferta/${h(o.id)}">${h(o.title)}</a></h3><p class="description">${h(o.description)}</p><div class="card-price">${money(o.price_ars*100)} <small>/ bloque de 15 min</small></div><div class="row spread"><span class="availability ${o.ready?'':'waiting'}">${o.ready?'● Cuenta vinculada':'○ Activación pendiente'}</span><button class="compare-button" data-action="compare" data-id="${h(o.id)}" aria-pressed="${selected}">${icon(selected?'check':'compare')}${selected?'Agregada':'Comparar'}</button></div><div class="card-bottom"><span class="small muted">${duration(o.blocks)} habitual</span>${button('Ver servicio '+icon('arrow'),'quick',o.id,'secondary small')}</div></div></article>`;}
function categories(ctx,active='',onExplore=false){return `<div class="category-strip" aria-label="Categorías">${['Todas',...ctx.data.categories].map(c=>`<button class="category-pill ${(c==='Todas'&&!active)||c===active?'active':''}" data-action="category" data-id="${h(c==='Todas'?'':c)}" ${onExplore?'data-local="1"':''}>${icon(c==='Todas'?'spark':categoryIcon(c))}${h(c)}</button>`).join('')}</div>`;}
function home(ctx){const offers=ctx.data.offers.filter(o=>o.active&&o.provider_id!==ctx.user?.id);const first=ctx.user?.name.split(' ')[0]||'tu próxima conexión';const completed=ctx.data.bookings.filter(b=>b.status==='COMPLETED').length,active=ctx.data.bookings.filter(b=>!['COMPLETED','CANCELLED'].includes(b.status));return `${head(ctx.user?'Hola, '+first+'.':'Conectá con lo que necesitás.','Tu tiempo tiene valor. Encontrá a alguien que te ayude a avanzar.',link(icon('plus')+' Ofrecer un servicio','/publicar',''),ctx.user?'BUENO VERTE POR ACÁ':'HECHO DE PERSONAS')}<section class="hero" data-enter><div class="hero-copy"><div class="eyebrow">${icon('spark')}TU PRÓXIMA BUENA CONEXIÓN</div><h2>Pequeños bloques.<br>Grandes soluciones.</h2><p>Contratá ayuda en bloques de 15 minutos. Tres tarifas claras. Personas con algo valioso para ofrecer.</p><div class="actions">${link('Encontrá tu servicio '+icon('arrow'),'/explorar','white')}${button('Cómo funciona','how','','soft-white')}</div></div><div class="hero-art" aria-hidden="true"><div class="orb"></div><div class="orb two"></div><img class="hero-person" src="/assets/photos/portrait-ana.webp" alt=""><img class="hero-person second" src="/assets/photos/portrait-mateo.webp" alt=""><span class="floating-tag top">${icon('clock')}15 min para avanzar</span><span class="floating-tag bottom">${icon('shield')}Precios claros, sin sorpresas</span>${icon('spark','soft-spark')}</div></section><div class="balance-row"><div class="mini-stat"><span class="stat-icon">${icon('clock')}</span><div><strong>15 min</strong><small>Cada bloque de servicio</small></div></div><div class="mini-stat"><span class="stat-icon">${icon('wallet')}</span><div><strong>3 tarifas</strong><small>$1.000 · $2.500 · $5.000</small></div></div><div class="mini-stat"><span class="stat-icon">${icon('check')}</span><div><strong>${ctx.user?completed:'5%'}</strong><small>${ctx.user?'Servicios completados':'Comisión de INTERA sobre el bruto'}</small></div></div></div>${ctx.config.mode==='mock'?`<div class="demo-guide"><div><strong>Probá ambos lados del nuevo modelo.</strong><p>Primero activá a Mateo como prestador. Después entrá como Ana para solicitar y pagar en el simulador.</p></div><div class="actions">${button('1 · Activar a Mateo','demo-user','demo_mateo','secondary small')}${button('2 · Contratar como Ana','demo-user','demo_ana','ghost small')}</div></div>`:''}<section class="section"><div class="section-head"><h2>¿Qué te gustaría resolver?</h2><a href="#/explorar">Ver todas ${icon('arrow','tiny-icon')}</a></div>${categories(ctx)}</section><section class="section"><div class="section-head"><h2>Habilidades que conectan</h2><span class="small muted">${offers.length} servicios publicados</span></div>${offers.length?`<div class="offer-grid">${offers.slice(0,6).map(o=>offerCard(ctx,o)).join('')}</div>`:empty('La comunidad empieza con vos','Publicá tu primera habilidad o dejá una necesidad para encontrar una buena conexión.',link('Ofrecer un servicio','/publicar',''))}</section><section class="section split"><div class="panel"><div class="section-head"><h2>Tu próxima acción</h2><a href="#/ordenes">Ver servicios ${icon('arrow','tiny-icon')}</a></div>${active.length?active.slice(0,3).map(b=>`<a class="list-row" href="#/orden/${h(b.id)}">${avatar(person(ctx,b.buyer_id===ctx.user.id?b.seller_id:b.buyer_id))}<div class="grow"><strong>${h(b.title)}</strong><small>${h(statusLabel(b.status))}</small></div>${icon('chevron')}</a>`):`<p class="muted small">${ctx.user?'No tenés acciones pendientes. Explorá una habilidad o compartí la tuya.':'Ingresá para organizar tus solicitudes y próximos servicios.'}</p>${link('Explorar servicios','/explorar','ghost small')}`}</div><div class="panel accent"><h2>Así de simple.</h2>${[['Elegí una habilidad','Cada prestador elige una de las tres tarifas por bloque.'],['Acordá y pagá','El prestador acepta. Luego pagás mediante Mercado Pago.'],['Recibí ayuda y valorá','Al terminar, confirmás el servicio y compartís tu experiencia.']].map(([t,d],i)=>`<div class="step-tile"><span class="step-num">${i+1}</span><div><strong class="small">${t}</strong><p>${d}</p></div></div>`).join('')}</div></section>`;}
function filteredOffers(ctx){const f=ctx.filters;let offers=ctx.data.offers.filter(o=>o.active&&(!f.category||o.category===f.category)&&(!f.price||o.price_ars===Number(f.price))&&(!f.neighborhood||o.neighborhood===f.neighborhood)&&(!f.saved||ctx.data.favorites.includes(o.id))&&(!f.q||normal(o.title+' '+o.description+' '+o.provider_name+' '+o.category).includes(normal(f.q))));if(f.sort==='price')offers.sort((a,b)=>a.price_ars-b.price_ars);if(f.sort==='duration')offers.sort((a,b)=>a.blocks-b.blocks);return offers;}
function explore(ctx){const f=ctx.filters,offers=filteredOffers(ctx);return `${head('Encontrá tu próxima conexión.','Ayuda concreta, en el tiempo que necesitás. Siempre sabés cuánto vas a pagar.',button(icon('heart')+' Guardados','show-saved','','secondary',`aria-pressed="${!!f.saved}"`),'EXPLORAR')} ${categories(ctx,f.category,true)}<form class="top-filters" data-form="filters"><label class="search-input"><span class="sr-only" hidden>Buscar servicios</span>${icon('search')}<input name="q" aria-label="Buscar servicios" placeholder="Una habilidad, una idea, algo por resolver…" value="${h(f.q||'')}"></label><select name="price" aria-label="Precio por bloque"><option value="">Todas las tarifas</option>${[1000,2500,5000].map(p=>`<option value="${p}" ${Number(f.price)===p?'selected':''}>${money(p*100)} / bloque</option>`).join('')}</select><select name="neighborhood" aria-label="Barrio"><option value="">Todos los barrios</option>${ctx.data.neighborhoods.map(n=>`<option ${f.neighborhood===n?'selected':''}>${h(n)}</option>`).join('')}</select><select name="sort" aria-label="Ordenar"><option value="">Más recientes</option><option value="price" ${f.sort==='price'?'selected':''}>Menor precio</option><option value="duration" ${f.sort==='duration'?'selected':''}>Menor duración</option></select><button class="btn" type="submit">${icon('filter')}Filtrar</button></form><div class="section-head"><span class="small muted">${offers.length} resultado${offers.length===1?'':'s'}${f.saved?' guardado'+(offers.length===1?'':'s'):''} · solo servicios remotos para adultos</span>${button('Limpiar filtros','clear-filters','','ghost small')}</div>${offers.length?`<div class="offer-grid">${offers.map(o=>offerCard(ctx,o)).join('')}</div>`:empty('Todavía no hay una coincidencia','Probá otra búsqueda o publicá qué necesitás.',button('Limpiar filtros','clear-filters','','')+' '+link('Publicar necesidad','/necesidades','secondary'))}`;}
function offerDetail(ctx,o){if(!o)return empty('Publicación no disponible','Puede estar pausada o no ser visible para tu cuenta.',link('Volver a explorar','/explorar',''));const own=o.provider_id===ctx.user?.id,p=person(ctx,o.provider_id),src=photoFor(o);return `${head('Un poco de ayuda. Mucho por avanzar.','Conocé la propuesta antes de acordar el servicio.',link(icon('arrow')+' Explorar','/explorar','secondary small'),'DETALLE DEL SERVICIO')}<div class="split"><section><div class="panel"><div class="detail-heading">${src?`<img class="thumb" src="${src}" alt="Imagen ilustrativa de demostración">`:icon(categoryIcon(o.category))}<div><div class="eyebrow">${h(o.category)}</div><h1>${h(o.title)}</h1><p>${icon('tech','tiny-icon')} Remoto · ${h(o.neighborhood)} · 18+</p></div></div><h3>Qué podemos resolver</h3><p style="white-space:pre-wrap">${h(o.description)}</p><div class="row"><span class="badge neutral">${icon('clock','tiny-icon')}${duration(o.blocks)} habitual</span><span class="badge neutral">${h(o.availability)}</span></div></div><div class="panel section"><div class="party">${avatar(p,'lg')}<div><h3>${h(p.name)}</h3><p class="small muted">${h(p.neighborhood)}${p.is_demo?' · Perfil de ejemplo':''}</p></div></div><p class="muted small" style="margin-top:17px">${h(p.bio)}</p><div class="row">${link('Ver perfil','/perfil/'+p.id,'secondary small')}${!own?button('Reportar','report-profile',p.id,'ghost small'):''}</div></div></section><aside class="panel sticky-panel"><div class="eyebrow">TU SERVICIO, A TU RITMO</div><div class="card-price" style="font-size:37px">${money(o.price_ars*100)} <small>/ bloque de 15 minutos</small></div>${quoteBox(o.price_ars,o.blocks)}${own?link('Editar mi publicación','/publicar?edit='+o.id,'full'):o.ready?button('Acordar este servicio '+icon('arrow'),'book',o.id,'full'):notice('El prestador todavía debe aceptar las condiciones y vincular su cuenta antes de recibir contrataciones.','warning')}<div class="row" style="margin-top:12px">${button(icon('heart')+' Guardar','favorite',o.id,'secondary small')}${button(icon('compare')+' Comparar','compare',o.id,'ghost small')}</div><p class="small muted" style="margin-top:16px">Primero enviás una solicitud. El pago se habilita cuando el prestador acepta. No compartas contraseñas ni datos de tarjetas por chat.</p></aside></div>`;}
function publish(ctx,o=null){if(!ctx.user)return accessRequired('Compartí lo que sabés hacer.');const p=o?.price_ars||2500,b=o?.blocks||2;return `${head(o?'Editá tu propuesta.':'Algo que sabés hacer. Alguien que lo necesita.','Elegí una de las tres tarifas. Las órdenes ya pactadas conservarán su precio.',link('Mi espacio de prestador','/prestador','secondary small'),'OFRECER UN SERVICIO')}<div class="split"><section class="panel"><form data-form="publish" data-id="${h(o?.id||'')}"><label class="field">Título del servicio<input name="title" required minlength="6" maxlength="100" value="${h(o?.title||'')}" placeholder="Por ejemplo: te ayudo a ordenar tu Excel"></label><div class="form-grid"><label class="field">Categoría<select name="category">${ctx.data.categories.map(c=>`<option ${o?.category===c?'selected':''}>${h(c)}</option>`).join('')}</select></label><label class="field">Barrio de tu perfil<select name="neighborhood">${ctx.data.neighborhoods.map(n=>`<option ${(o?.neighborhood||ctx.user.neighborhood)===n?'selected':''}>${h(n)}</option>`).join('')}</select></label></div><label class="field">Qué incluye y qué no incluye<textarea name="description" required minlength="20" maxlength="2000" rows="5" placeholder="Contá qué van a trabajar, qué necesitás y qué se lleva la otra persona.">${h(o?.description||'')}</textarea></label><label class="field">Disponibilidad orientativa<input name="availability" maxlength="200" required value="${h(o?.availability||'Días hábiles · horario a coordinar')}"></label><div class="eyebrow">TU TARIFA POR CADA BLOQUE DE 15 MINUTOS</div><div class="price-options">${[1000,2500,5000].map(v=>`<label class="price-option"><input type="radio" name="priceArs" value="${v}" ${v===p?'checked':''}><span>${money(v*100)}<small>ARS / 15 minutos</small></span></label>`).join('')}</div><label class="field">Duración habitual, en bloques<input name="blocks" type="number" min="1" max="32" step="1" value="${b}" required><small>1 bloque = 15 minutos. De 1 a 32 bloques. La duración se confirma en cada orden.</small></label>${notice('Esta entrega habilita servicios remotos para personas adultas. No se ofrecen atención de salud, cuidados, tareas con menores ni profesiones reguladas.')}<label class="check-label"><input type="checkbox" name="sellerFeeAccepted" required><span>Acepto que INTERA descuente el <strong>5% del bruto</strong> por intermediación. Los cargos de Mercado Pago y las retenciones aplicables se descuentan por separado. Me corresponde informar y emitir los comprobantes de mi servicio.</span></label><div class="form-error" role="alert"></div><button type="submit" class="btn full">${icon('plus')}${o?'Guardar cambios':'Publicar mi servicio'}</button></form></section><aside class="panel sticky-panel"><div class="eyebrow">VISTA PREVIA · CÁLCULO ORIENTATIVO</div><h2 id="preview-title">${h(o?.title||'Tu próxima propuesta')}</h2><p class="muted small">El servidor valida el importe y guarda una cotización inmutable al contratar.</p><div id="price-preview">${quoteBox(p,b,{seller:true})}</div>${notice('No se guarda un saldo en INTERA. El cobro se procesa en la cuenta del prestador, con Split 1:1 de Mercado Pago.')}<div class="small muted">${ctx.data.seller?.status==='CONNECTED'?'Tu cuenta de cobro está vinculada.':'Podés publicar ahora y vincular tu cuenta desde «Soy prestador» para recibir contrataciones.'}</div></aside></div>`;}
const accessRequired=(title='Tu próximo paso empieza acá.')=>head(title,'Ingresá para acceder a tus servicios, pagos y conversaciones.')+empty('Tu espacio es personal','Cada cuenta ve únicamente sus propias operaciones.',link('Ingresar','/acceso','')+' '+link('Crear cuenta','/registro','secondary'),'user');
function orders(ctx){if(!ctx.user)return accessRequired();const filtered=ctx.data.bookings.filter(b=>ctx.orderFilter==='all'||ctx.orderFilter==='buyer'&&b.buyer_id===ctx.user.id||ctx.orderFilter==='seller'&&b.seller_id===ctx.user.id||ctx.orderFilter==='active'&&!['COMPLETED','CANCELLED'].includes(b.status));return `${head('Mis servicios','Un lugar para acordar, conversar y seguir cada contratación.',button(icon('refresh')+' Actualizar','refresh','','secondary small'),'TU ACTIVIDAD')}<div class="category-strip">${[['all','Todos'],['buyer','Solicitados'],['seller','Como prestador'],['active','En curso o pendientes']].map(([v,t])=>button(t,'order-filter',v,ctx.orderFilter===v?'':'secondary',`aria-pressed="${ctx.orderFilter===v}"`)).join('')}</div><section class="section">${filtered.length?filtered.map(b=>orderCard(ctx,b)).join(''):empty('Todavía no hay servicios acá','Encontrá una habilidad y enviá tu primera solicitud.',link('Explorar servicios','/explorar',''),'swap')}</section>`;}
function orderCard(ctx,b){return `<article class="order-card" data-enter><div class="row spread"><div><div class="row">${badge(b.status)}${badge(b.payment_status)}</div><h3><a href="#/orden/${h(b.id)}">${h(b.title)}</a></h3><div class="order-meta"><span>${icon('clock','tiny-icon')}${duration(b.blocks)}</span><span>${icon('calendar','tiny-icon')}${dateLabel(b.scheduled_at)}</span><span>${b.buyer_id===ctx.user.id?'Con '+h(b.seller_name):'Para '+h(b.buyer_name)}</span></div></div><div class="order-price"><strong>${money(b.gross_cents)}</strong><small class="muted">${b.blocks} × ${money(b.price_ars*100)}</small></div></div><div class="actions">${link('Ver orden '+icon('arrow'),'/orden/'+b.id,'secondary small')}${b.status==='REQUESTED'&&b.seller_id===ctx.user.id?button('Aceptar solicitud','accept',b.id,'small'):''}${b.status==='ACCEPTED'&&b.buyer_id===ctx.user.id&&['UNPAID','CHECKOUT_CREATED','PENDING','REJECTED'].includes(b.payment_status)?button('Ver pago','checkout',b.id,'small'):''}</div></article>`;}
function orderDetail(ctx,d){const b=d.booking,seller=b.seller_id===ctx.user.id,other=seller?b.buyer_id:b.seller_id;const idx={REQUESTED:0,ACCEPTED:1,IN_PROGRESS:2,COMPLETED_BY_PROVIDER:3,COMPLETED:4}[b.status]??-1;let actionText='',actionButtons='';
 if(b.status==='REQUESTED'){actionText=seller?'Revisá duración, horario y alcance. Si aceptás, se habilitará el pago al solicitante.':'Tu solicitud está enviada. El prestador debe aceptarla antes de que puedas pagar.';if(seller)actionButtons=button('Aceptar servicio '+icon('check'),'accept',b.id,'');}
 else if(b.status==='ACCEPTED'&&b.payment_status==='APPROVED'){actionText=seller?'El pago está aprobado. Iniciá cuando comience el servicio. La disponibilidad del dinero se consulta en Mercado Pago.':'El pago está aprobado. Coordiná los últimos detalles con el prestador por chat.';if(seller)actionButtons=button('Iniciar servicio','start',b.id,'');}
 else if(b.status==='ACCEPTED'&&['UNPAID','CHECKOUT_CREATED','PENDING','REJECTED'].includes(b.payment_status)){actionText=seller?'Aceptaste la solicitud. Esperá un pago aprobado antes de prestar el servicio.':'El prestador aceptó. Revisá el total y continuá al checkout. Un pago pendiente no confirma la contratación.';if(!seller)actionButtons=button('Revisar y pagar '+icon('arrow'),'checkout',b.id,'');}
 else if(b.status==='IN_PROGRESS'){actionText=seller?'El servicio está en curso. Al finalizar, marcá tu parte completada.':'El servicio está en curso. Si necesitás más tiempo, pedile al prestador una ampliación con precio explícito.';if(seller)actionButtons=button('Marcar servicio realizado','complete',b.id,'');}
 else if(b.status==='COMPLETED_BY_PROVIDER'){actionText=seller?'Marcaste tu parte completada. Falta la confirmación del solicitante.':'El prestador marcó su parte realizada. Confirmá solo si recibiste el servicio o abrí un reclamo.';if(!seller)actionButtons=button('Confirmar servicio recibido '+icon('check'),'confirm',b.id,'');}
 else if(b.status==='COMPLETED'){actionText='Servicio completado. Tu valoración ayuda a que la comunidad tenga información sobre cada experiencia.';if(!d.reviews.some(r=>r.author_id===ctx.user.id)&&b.payment_status==='APPROVED')actionButtons=button('Valorar experiencia '+icon('star'),'review',b.id,'');}
 else if(b.status==='CANCELLED'){actionText='Esta orden está cancelada. Si hubo un pago aprobado, la devolución se solicita a Mercado Pago y se confirma por separado.';}
 else{actionText='Esta operación necesita revisión. Consultá los estados de pago y los reclamos, sin asumir que el dinero está disponible.';}
 if(['CHARGED_BACK','MEDIATION','PAYMENT_REVIEW','REFUND_PENDING','PARTIALLY_REFUNDED'].includes(b.payment_status)){actionText='Hay una incidencia o devolución en el pago. No continúes una prestación hasta aclarar su estado.';actionButtons='';}
 return `${head('Todo sobre tu servicio.','El estado de la prestación y el del pago son independientes.',link('Mis servicios','/ordenes','secondary small'),'ORDEN · '+b.id.slice(0,8).toUpperCase())}<div class="split"><section><div class="panel"><div class="row">${badge(b.status)}${badge(b.payment_status)}${b.kind==='EXTENSION'?'<span class="badge">Ampliación</span>':''}</div><h2 style="margin-top:17px">${h(b.title)}</h2><div class="row muted small">${icon('calendar','tiny-icon')}${dateLabel(b.scheduled_at)} · ${duration(b.blocks)} · Remoto</div><div class="party-row"><a class="party" href="#/perfil/${h(b.buyer_id)}">${avatar(person(ctx,b.buyer_id))}<span><strong>${h(b.buyer_name)}</strong><small>Solicita</small></span></a>${icon('swap')}<a class="party" href="#/perfil/${h(b.seller_id)}">${avatar(person(ctx,b.seller_id))}<span><strong>${h(b.seller_name)}</strong><small>Presta el servicio</small></span></a></div>${b.notes?`<p class="small muted" style="white-space:pre-wrap">${h(b.notes)}</p>`:''}<div class="flow-track">${['Solicitud','Aceptación','En curso','Realizado','Confirmado'].map((t,i)=>`<div class="flow-step ${i<idx?'done':i===idx?'current':''}"><span>${i<idx?icon('check','tiny-icon'):i+1}</span>${t}</div>`).join('')}</div><div class="action-panel"><h3>Tu próximo paso</h3><p>${h(actionText)}</p>${actionButtons}</div><div class="actions">${['REQUESTED','ACCEPTED'].includes(b.status)?button('Cancelar','cancel',b.id,'danger small'):''}${button('Pedir ayuda / reclamar','report-order',b.id,'secondary small')}${seller&&!b.parent_id&&['ACCEPTED','IN_PROGRESS'].includes(b.status)&&b.payment_status==='APPROVED'?button('+ 15 min o más','extension',b.id,'ghost small'):''}${button(icon('download')+' Calendario','export-order',b.id,'ghost small')}</div>${b.cancel_reason?notice(h(b.cancel_reason),'warning'):''}</div><div class="panel chat-panel section"><div class="chat-head"><h3>${icon('chat')}Conversación con ${h(person(ctx,other).name.split(' ')[0])}</h3><p class="small muted">Mensajes privados de esta orden. No compartas claves, documentos ni tarjetas.</p></div><div class="messages" id="messages">${d.messages.length?d.messages.map(m=>`<div class="message ${m.sender_id===ctx.user.id?'mine':''}"><p>${h(m.body)}</p><small>${h(m.sender_name)} · ${dateLabel(m.created_at)}</small></div>`).join(''):'<p class="small muted">Podés empezar por confirmar el alcance y cómo van a conectarse.</p>'}</div><form class="chat-form" data-form="chat" data-id="${h(b.id)}"><div class="suggestions"><button type="button" data-action="draft" data-text="¡Hola! ¿Coordinamos los detalles del servicio?">Coordinar detalles</button><button type="button" data-action="draft" data-text="¿Podemos confirmar qué incluye el servicio?">Confirmar alcance</button><button type="button" data-action="emoji" data-text="👍">👍</button><button type="button" data-action="emoji" data-text="¡Gracias!">¡Gracias!</button></div><div class="chat-compose"><textarea name="body" aria-label="Mensaje" placeholder="Escribí un mensaje…" required maxlength="2000" rows="1"></textarea><button class="btn" type="submit" aria-label="Enviar mensaje">${icon('send')}<span class="send-label">Enviar</span></button></div><div class="chat-counter" id="chat-counter">0 / 2000</div><div class="form-error" role="alert"></div></form></div>${d.children.length?`<section class="panel section"><h3>Ampliaciones con pago separado</h3>${d.children.map(c=>`<a class="list-row" href="#/orden/${h(c.id)}"><div class="grow"><strong>${c.blocks} bloque${c.blocks===1?'':'s'} · ${money(c.gross_cents)}</strong><small>${h(statusLabel(c.payment_status))}</small></div>${icon('chevron')}</a>`).join('')}</section>`:''}${d.reviews.length?`<section class="panel section"><h3>Experiencias compartidas</h3>${notice(d.reviewsRevealed?'Las valoraciones ya se pueden consultar.':'Solo vos podés ver tu valoración. Se revela cuando ambas partes evalúan o transcurren 7 días.')} ${d.reviews.map(r=>`<div class="review-row"><strong>${h(person(ctx,r.author_id).name)}</strong> <span class="review-stars">${'★'.repeat(r.rating)}</span><p>${h(r.comment||'Sin comentario adicional.')}</p></div>`).join('')}</section>`:''}</section><aside><div class="panel"><div class="eyebrow">PRECIO ACORDADO · ARS</div>${quoteBox(b.price_ars,b.blocks)}${notice('Confirmar el servicio no libera dinero. La acreditación y disponibilidad siguen las reglas y plazos de Mercado Pago.')}<div class="small muted">Condiciones aceptadas al contratar:<br><code>${h(b.terms_hash.slice(0,16))}…</code></div></div><div class="panel section"><div class="section-head"><h2>Estado del pago</h2>${button(icon('refresh'),'reconcile',b.id,'secondary small','aria-label="Actualizar pago"')}</div>${d.payments.length?d.payments.map(p=>`<div class="payment-box"><div class="row spread">${badge(p.status.toUpperCase())}<strong>${money(p.amount_cents)}</strong></div><p style="margin-top:12px">Pago #${h(p.id)}<br>Disponibilidad MP: <strong>${h(p.release_status==='pending'?'Pendiente':p.release_status==='released'?'Liberado por MP':p.release_status==='unreported'?'No informada':p.release_status)}</strong>${p.release_date?'<br>Fecha informada: '+dateLabel(p.release_date):''}</p><p>Comisión pactada: ${money(b.commission_cents)}<br>Comisión reportada por MP: ${p.commission_reported_cents===null?'No informada':money(p.commission_reported_cents)}<br>Cargo de procesamiento reportado: ${p.processor_fee_cents===null?'No informado':money(p.processor_fee_cents)}<br>Neto reportado por MP: ${p.net_reported_cents===null?'No informado':money(p.net_reported_cents)}${p.refunded_cents?'<br>Reembolsado: '+money(p.refunded_cents):''}</p></div>`).join(''):'<p class="small muted">Todavía no hay un pago verificado para esta orden. Volver del checkout no acredita por sí solo un pago.</p>'}${d.refunds.map(r=>`<div class="notice warning"><div><strong>Devolución · ${money(r.amount_cents)}</strong><br>${h(statusLabel(r.status))}<br>${h(r.reason)}</div></div>`).join('')}</div>${d.reports.length?`<div class="panel section"><h3>Tus reclamos</h3>${d.reports.map(r=>`<div class="review-row">${badge(r.status)}<p class="small">${h(r.body)}</p>${r.resolution?notice(h(r.resolution)):''}<small class="muted">${h(r.id.slice(0,8))}</small></div>`).join('')}</div>`:''}</aside></div>`;
}
function checkout(ctx,d){const b=d.booking;if(ctx.user?.id!==b.buyer_id)return accessRequired('Este pago corresponde al solicitante.');return `${head('Un último vistazo antes de pagar.','El precio está acordado. Vos decidís cuándo continuar.',link('Volver a la orden','/orden/'+b.id,'secondary small'),'CHECKOUT')}<div class="split"><section class="panel"><div class="party">${avatar(person(ctx,b.seller_id),'lg')}<div><h2>${h(b.title)}</h2><p class="small muted">Con ${h(b.seller_name)} · ${dateLabel(b.scheduled_at)}</p></div></div>${quoteBox(b.price_ars,b.blocks)}${notice('Mercado Pago procesa el cobro y distribuye el importe. INTERA no guarda tu tarjeta ni mantiene una billetera propia.')}<form data-form="checkout" data-id="${h(b.id)}"><label class="check-label"><input type="checkbox" name="accept" required><span>Revisé <strong>${b.blocks} bloques (${duration(b.blocks)}) por ${money(b.gross_cents)}</strong>. Acepto las <a class="text-link" href="#/condiciones" target="_blank" rel="noopener">condiciones vigentes</a>, la comisión del 5% descontada al prestador y los cargos externos aplicables. No se agrega un 5% extra a este total.</span></label><div class="form-error" role="alert"></div><button type="submit" class="btn full">${icon('shield')}${ctx.config.mode==='mock'?'Abrir simulador de pago':'Continuar a Mercado Pago'}</button></form><div id="simulation-slot"></div></section><aside class="panel soft"><div class="eyebrow">PAGO Y SERVICIO, POR SEPARADO</div><h2>Todo claro desde el principio.</h2><div class="step-tile"><span class="step-num">1</span><div><strong class="small">El prestador acepta</strong><p>La tarifa y la duración quedan guardadas en la orden.</p></div></div><div class="step-tile"><span class="step-num">2</span><div><strong class="small">Mercado Pago confirma</strong><p>Un pago pendiente o rechazado no habilita el servicio.</p></div></div><div class="step-tile"><span class="step-num">3</span><div><strong class="small">La acreditación depende de MP</strong><p>La fecha de liberación no depende del botón de finalización de INTERA.</p></div></div>${ctx.config.mode==='mock'?notice('<strong>Simulación local.</strong> No se abre Mercado Pago ni se mueve dinero real.','warning'):notice('No cierres la orden: podés regresar para consultar el estado aunque se interrumpa la conexión.')}</aside></div>`;}
function seller(ctx){if(!ctx.user)return accessRequired('Compartí tu talento. Cobrá con claridad.');const account=ctx.data.seller,own=ctx.data.offers.filter(o=>o.provider_id===ctx.user.id),pending=ctx.data.bookings.filter(b=>b.seller_id===ctx.user.id&&b.status==='REQUESTED');return `${head('Tu espacio de prestador.','Elegí la tarifa de cada publicación. INTERA intermedia y descuenta el 5% del bruto.',link(icon('plus')+' Nuevo servicio','/publicar',''),'SOY PRESTADOR')}<div class="split"><section class="panel"><div class="row spread"><span class="stat-icon">${icon('link')}</span>${badge(account?.status||'Sin vincular',account?'':'warning')}</div><h2 style="margin-top:18px">Tu cuenta de Mercado Pago.</h2><p class="muted small">Autorizás a INTERA mediante OAuth para crear checkouts y conciliar tus operaciones. No te pedimos la contraseña de Mercado Pago.</p>${account?`<p class="small">Cuenta vinculada: <strong>${h(account.mp_user_id)}</strong><br>Ambiente: <strong>${h(account.mode)}</strong></p>`:''}<div class="actions">${button(icon('link')+' '+(account?.status==='CONNECTED'?'Renovar vinculación':'Vincular Mercado Pago'),'mp-connect','','')}${account?.status==='CONNECTED'?button('Pausar nuevos cobros','mp-pause','','secondary'):''}</div>${ctx.config.mode==='mock'?notice('La vinculación es ficticia en este simulador. En sandbox/live se abre la autorización oficial de Mercado Pago.','warning'):''}</section><aside class="panel soft"><div class="eyebrow">COMISIÓN TOTAL DE INTERMEDIACIÓN</div><h2>5% del bruto.</h2><p class="small muted">$50 por cada $1.000 del servicio, descontados al prestador. No es una tarifa adicional para el cliente.</p><div class="table-wrap"><table><thead><tr><th>Por bloque</th><th>INTERA</th><th>Antes de MP</th></tr></thead><tbody>${[1000,2500,5000].map(p=>`<tr><td>${money(p*100)}</td><td>${money(p*5)}</td><td>${money(p*95)}</td></tr>`).join('')}</tbody></table></div><p class="small muted" style="margin-top:14px">La columna final no es el neto a recibir. Faltan cargos de procesamiento y retenciones que correspondan.</p></aside></div>${pending.length?`<section class="section"><div class="section-head"><h2>Solicitudes para vos</h2>${link('Todas','/ordenes','ghost small')}</div>${pending.map(b=>orderCard(ctx,b)).join('')}</section>`:''}<section class="section"><div class="section-head"><h2>Mis publicaciones</h2><a href="#/movimientos">Ver pagos y cobros ${icon('arrow','tiny-icon')}</a></div>${own.length?own.map(o=>`<article class="order-card"><div class="row spread"><div><div class="row"><span class="badge ${o.active?'good':'neutral'}">${o.active?'Publicada':'Pausada'}</span><span class="small muted">${h(o.category)}</span></div><h3>${h(o.title)}</h3><span class="small muted">${money(o.price_ars*100)} / 15 min · ${duration(o.blocks)} habitual</span></div><div class="actions">${link('Editar','/publicar?edit='+o.id,'secondary small')}${button(o.active?'Pausar':'Reactivar','toggle-offer',o.id,'ghost small',`data-active="${o.active?'0':'1'}"`)}</div></div></article>`).join(''):empty('Tu primera habilidad puede ayudar a alguien','Publicá con alcance claro y elegí una tarifa.',link('Publicar un servicio','/publicar',''),'briefcase')}</section>`;}
function needs(ctx){return `${head('¿Qué necesita la comunidad?','Las necesidades orientan la búsqueda. No generan cobros ni créditos.',button(icon('plus')+' Publicar necesidad','need','',''),'NECESIDADES')}<div class="offer-grid">${ctx.data.needs.map(n=>`<article class="panel" data-enter><div class="eyebrow">${icon(categoryIcon(n.category))}${h(n.category)}</div><h3>${h(n.title)}</h3><p class="small muted">${h(n.description)}</p><div class="provider-line">${avatar(person(ctx,n.user_id))}${h(n.requester_name)} · ${h(n.neighborhood)}</div><strong>${money(n.max_price_ars*100)} / bloque máximo</strong><p class="small muted">${n.blocks} bloques · ${duration(n.blocks)}</p><div class="actions">${button('Buscar coincidencias','need-match',n.id,'secondary small')}${n.user_id===ctx.user?.id?button('Cerrar necesidad','need-close',n.id,'ghost small'):''}</div></article>`).join('')}</div>${!ctx.data.needs.length?empty('Todavía no hay necesidades publicadas','Contá qué querés resolver. No se cobra al publicar.',button('Publicar necesidad','need','',''),'list'):''}`;}
function profile(ctx,p,own=false){if(!p)return accessRequired('Tu espacio, tus conexiones.');const offers=ctx.data.offers.filter(o=>o.provider_id===p.id&&o.active);return `${head(own?'Mi espacio.':'Conocé a '+p.name.split(' ')[0]+'.','La identidad de cada prestación empieza con información clara.',own?button(icon('logout')+' Cerrar sesión','logout','','secondary small'):'','PERFIL')}<section class="panel" style="padding:0"><div class="profile-cover"></div><div class="profile-info">${avatar(p,'xl')}<h1>${h(p.name)}</h1><p>${icon('pin','tiny-icon')}${h(p.neighborhood)}${p.is_demo?' · Perfil ilustrativo de demostración':''}</p><p>${h(p.bio||'Esta persona todavía no agregó una presentación.')}</p>${!own?`<div class="actions">${button('Reportar perfil','report-profile',p.id,'secondary small')}${button('Bloquear / desbloquear','block',p.id,'ghost small')}</div>`:''}</div></section>${own?`<section class="section split"><div class="panel"><h2>Lo que compartís.</h2><form data-form="profile"><label class="field">Nombre<input name="name" value="${h(p.name)}" required minlength="2" maxlength="80"></label><label class="field">Barrio<select name="neighborhood">${ctx.data.neighborhoods.map(n=>`<option ${n===p.neighborhood?'selected':''}>${h(n)}</option>`).join('')}</select></label><label class="field">Presentación<textarea name="bio" maxlength="1000">${h(p.bio)}</textarea></label><div class="form-error" role="alert"></div><button class="btn" type="submit">Guardar mi perfil</button></form></div><aside class="panel"><h2>Tu información, a mano.</h2>${[['/prestador','briefcase','Mi espacio de prestador'],['/movimientos','wallet','Pagos y cobros'],['/agenda','calendar','Mi agenda'],['/necesidades','list','Necesidades de la comunidad'],['/ayuda','help','Ayuda y privacidad']].map(([path,i,t])=>`<a class="list-row" href="#${path}">${icon(i)}<div class="grow"><strong>${t}</strong></div>${icon('chevron')}</a>`).join('')}<div class="actions section">${button(icon('download')+' Exportar mis datos','export-data','','secondary small')}${button('Experiencia visual','settings','','ghost small')}</div><p class="small muted" style="margin-top:17px">Los datos de v1–v4 no se importan como dinero. La exportación es personal y no es una factura.</p></aside></section>`:''}<section class="section"><div class="section-head"><h2>${own?'Mis servicios publicados':'Lo que '+h(p.name.split(' ')[0])+' ofrece'}</h2></div>${offers.length?`<div class="offer-grid">${offers.map(o=>offerCard(ctx,o)).join('')}</div>`:empty('Sin publicaciones activas','Las nuevas habilidades aparecerán acá.',own?link('Publicar servicio','/publicar',''):'','briefcase')}</section>`;}
function finances(ctx,f){if(!ctx.user)return accessRequired();const selling=f.payments.filter(p=>p.seller_id===ctx.user.id),buying=f.payments.filter(p=>p.buyer_id===ctx.user.id);const sum=rows=>rows.filter(p=>['approved','refunded'].includes(p.status)).reduce((n,p)=>n+p.amount_cents-p.refunded_cents,0);return `${head('Tus pagos. Tus cobros.','Operaciones verificadas con el proveedor de pagos. Esto no es una billetera.',button(icon('download')+' Exportar CSV','export-finances','','secondary small'),'MOVIMIENTOS EN ARS')}<div class="balance-row"><div class="mini-stat"><span class="stat-icon">${icon('briefcase')}</span><div><strong>${money(sum(selling))}</strong><small>Bruto cobrado menos devoluciones · antes de cargos</small></div></div><div class="mini-stat"><span class="stat-icon">${icon('wallet')}</span><div><strong>${money(sum(buying))}</strong><small>Pagado menos devoluciones</small></div></div><div class="mini-stat"><span class="stat-icon">${icon('shield')}</span><div><strong>5%</strong><small>Comisión pactada sobre el bruto</small></div></div></div><section class="section">${notice('La aprobación de un pago no garantiza disponibilidad inmediata. El neto y la liberación informados por Mercado Pago se consultan en cada orden. Los contracargos no se incluyen en los totales confirmados.')} ${f.payments.length?`<div class="table-wrap"><table><thead><tr><th>Servicio / rol</th><th>Bruto</th><th>Comisión pactada</th><th>Reembolsado</th><th>Estado</th><th>Detalle</th></tr></thead><tbody>${f.payments.map(p=>`<tr><td>${h(p.title)}<br><small class="muted">${p.seller_id===ctx.user.id?'Como prestador':'Como solicitante'} · #${h(p.id)}</small></td><td>${money(p.amount_cents)}</td><td>${money(p.agreed_commission_cents)}</td><td>${money(p.refunded_cents)}</td><td>${badge(p.status.toUpperCase())}</td><td><a class="text-link" href="#/orden/${h(p.booking_id)}">Ver orden</a></td></tr>`).join('')}</tbody></table></div>`:empty('Todavía no hay pagos verificados','Un checkout creado o una solicitud aceptada no cuentan como un pago aprobado.',link('Mis servicios','/ordenes',''),'wallet')}</section>${f.entries.length?`<section class="section panel"><h3>Registro de conciliación</h3><p class="small muted">${f.entries.length/3} eventos económicos registrados con asientos compensatorios. Es un registro operativo, no contabilidad fiscal ni fondos custodiados por INTERA.</p>${button('Exportar todos mis registros','export-data','','secondary small')}</section>`:''}`;}
function agenda(ctx){if(!ctx.user)return accessRequired();const selected=ctx.calendarDate;const base=new Date(ctx.calendarYear,ctx.calendarMonth,1),days=new Date(ctx.calendarYear,ctx.calendarMonth+1,0).getDate(),start=(base.getDay()+6)%7;const all=ctx.data.bookings.filter(b=>!['REQUESTED','CANCELLED'].includes(b.status));const monthEvents=all.filter(b=>new Date(b.scheduled_at).getFullYear()===ctx.calendarYear&&new Date(b.scheduled_at).getMonth()===ctx.calendarMonth);const events=monthEvents.filter(b=>!selected||new Date(b.scheduled_at).getDate()===selected);const now=new Date();return `${head('Hacé lugar a lo que importa.','Tus acuerdos, organizados. Los horarios se muestran en la zona de este dispositivo.',button(icon('download')+' Exportar agenda','export-agenda','','secondary small'),'AGENDA')}<div class="split"><section class="panel"><div class="calendar-controls">${button(icon('chevron'),'month-prev','','secondary small','aria-label="Mes anterior"')}<h3>${h(base.toLocaleString('es-AR',{month:'long',year:'numeric'}))}</h3>${button(icon('chevron'),'month-next','','secondary small','aria-label="Mes siguiente"')}</div><div class="calendar-grid">${['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'].map(d=>`<div class="calendar-head">${d}</div>`).join('')}${Array.from({length:start},()=>'<div class="calendar-day filler"></div>').join('')}${Array.from({length:days},(_,i)=>{const d=i+1,n=monthEvents.filter(e=>new Date(e.scheduled_at).getDate()===d).length,today=now.getDate()===d&&now.getMonth()===ctx.calendarMonth&&now.getFullYear()===ctx.calendarYear;return `<button class="calendar-day ${selected===d?'active':''} ${today?'today':''}" data-action="calendar-day" data-id="${d}" aria-label="Día ${d}, ${n} servicios">${d}${n?'<span class="day-dot"></span><small>'+n+' servicio'+(n===1?'':'s')+'</small>':''}</button>`;}).join('')}</div><div style="margin-top:17px">${button('Ver todo el mes','calendar-day','','ghost small')}</div></section><aside><div class="section-head"><h2>${selected?'Día '+selected:'En este mes'}</h2><span class="small muted">${events.length} acuerdos</span></div>${events.length?events.map(b=>orderCard(ctx,b)).join(''):empty('Un poco de espacio libre','Los acuerdos aceptados aparecerán en la agenda. Revisá el estado de pago antes de comenzar.','','calendar')}</aside></div>`;}
function terms(ctx){return `${head('Reglas claras para conectar.','Precios, intermediación, pagos, reclamos y privacidad.',ctx.user?button('Aceptar condiciones','terms','',''):'','CONDICIONES DEL SERVICIO')}<section class="panel">${notice('Versión '+h(ctx.legal.version)+' · Huella del documento: <code>'+h(ctx.legal.hash.slice(0,20))+'…</code>')}<pre class="legal">${h(ctx.legal.content)}</pre></section>`;}
function help(ctx){return `${head('Estamos para ordenar el próximo paso.','Consultá tus operaciones, registrá un reclamo o solicitá acceso y supresión de datos.','','AYUDA Y PRIVACIDAD')}<div class="split"><section class="panel"><h2>Primero, lo importante.</h2>${notice(ctx.config.mode==='mock'?'Este es un entorno local. Los reclamos se guardan en la base de prueba: no hay una persona de soporte conectada.':'El reclamo queda registrado para el operador. No es un servicio de emergencia ni de atención inmediata.','warning')}<form data-form="report"><label class="field">Tipo de solicitud<select name="type"><option value="SUPPORT">Consulta o problema de uso</option><option value="PRIVACY_ACCESS">Acceso o rectificación de mis datos</option><option value="PRIVACY_DELETE">Solicitar eliminación de mi cuenta</option><option value="REFUND_REQUEST">Reclamo o solicitud de devolución</option><option value="WITHDRAWAL">Solicitud de arrepentimiento</option><option value="ABUSE">Abuso o conducta inapropiada</option></select></label><label class="field">Contanos qué necesitás<textarea name="body" minlength="10" maxlength="3000" required placeholder="Incluí la referencia de la orden, sin datos de tarjetas ni contraseñas."></textarea></label><div class="form-error" role="alert"></div><button class="btn" type="submit">Registrar solicitud</button></form>${ctx.config.supportEmail?`<p class="small muted" style="margin-top:16px">Canal del operador: ${h(ctx.config.supportEmail)}</p>`:''}</section><aside class="panel"><h2>Preguntas frecuentes.</h2>${[['¿Qué estoy contratando?','Un servicio con duración acordada en bloques de 15 minutos. La tarifa por bloque es $1.000, $2.500 o $5.000 ARS, elegida por el prestador.'],['¿El 5% se suma al precio?','No. La comisión de INTERA se descuenta del bruto del prestador. Los cargos de Mercado Pago y las retenciones son conceptos distintos.'],['¿Cuándo recibe su dinero el prestador?','Según el estado y plazo que informe Mercado Pago. La confirmación del servicio no libera dinero ni crea una transferencia.'],['¿Qué pasa si cancelo?','Antes de iniciar, se solicita el reembolso de los pagos aprobados. La devolución se marca confirmada al verificarse con Mercado Pago. Después del inicio, corresponde revisión. No se excluyen derechos imperativos.'],['¿Puedo importar mis créditos anteriores?','No. Los datos de versiones anteriores eran simulados; no se convierten en pesos ni en reputación real.']].map(([q,a])=>`<details class="review-row"><summary><strong class="small">${q}</strong></summary><p class="small muted" style="margin-top:10px">${a}</p></details>`).join('')}${link('Leer las condiciones completas','/condiciones','secondary small')}</aside></div>`;}
function authPage(ctx,kind='login'){const register=kind==='register',forgot=kind==='forgot',reset=kind==='reset',verify=kind==='verify';return `<div class="auth-wrap"><div class="brand"><img src="/assets/intera-mark.svg" alt="">intera</div><section class="panel"><div class="eyebrow">TU PRÓXIMA BUENA CONEXIÓN</div><h1>${register?'Todos tenemos algo para aportar.':forgot||reset?'Recuperá tu acceso.':verify?'Confirmá tu correo.':'Bueno tenerte acá.'}</h1><p class="small muted">${register?'Una cuenta para ofrecer, contratar y organizar tus servicios.':verify?'El enlace es de un solo uso y vence a los 30 minutos.':forgot||reset?'No compartas este enlace ni tu nueva contraseña.':'Ingresá a tu espacio personal.'}</p><form data-form="${kind}">${register?`<label class="field">Nombre y apellido<input name="name" required minlength="2" maxlength="80" autocomplete="name"></label>`:''}${!reset&&!verify?'<label class="field">Correo electrónico<input name="email" type="email" autocomplete="email" required maxlength="254"></label>':''}${!forgot&&!verify?`<label class="field">${reset?'Nueva contraseña':'Contraseña'}<input name="password" type="password" minlength="12" maxlength="128" required autocomplete="${register||reset?'new-password':'current-password'}"><small>${register||reset?'Al menos 12 caracteres. Usá una contraseña única.':''}</small></label>`:''}${register?`<div class="form-grid"><label class="field">Fecha de nacimiento<input name="dob" type="date" required><small>Exclusivamente 18+</small></label><label class="field">Barrio<select name="neighborhood">${ctx.data.neighborhoods.map(n=>`<option>${h(n)}</option>`).join('')}</select></label></div><label class="check-label"><input type="checkbox" name="conditions" required><span>Leí y acepto las <a class="text-link" href="#/condiciones" target="_blank" rel="noopener">condiciones del servicio y privacidad</a>, incluyendo el modelo de bloques pagos y la comisión del 5% para prestadores.</span></label>`:''}<div class="form-error" role="alert"></div><button type="submit" class="btn full">${register?'Crear mi cuenta':forgot?'Enviar instrucciones':reset?'Guardar nueva contraseña':verify?'Verificar mi correo':'Ingresar'}${icon('arrow')}</button></form><div id="auth-notice"></div>${!register&&!forgot&&!reset&&!verify?'<p class="auth-help"><a href="#/recuperar">Olvidé mi contraseña</a></p><p class="auth-help">¿Primera vez? <a href="#/registro">Creá tu cuenta</a></p>':'<p class="auth-help"><a href="#/acceso">Volver a ingresar</a></p>'}${ctx.config.mode==='mock'?`<div style="margin-top:22px">${button('Probar con perfiles de ejemplo','demo','','ghost full small')}</div>`:''}</section></div>`;}
function compareTray(ctx){const ids=ctx.compare.filter(id=>ctx.data.offers.some(o=>o.id===id));if(!ids.length)return '';return `<aside class="compare-tray" aria-label="Comparador"><span class="stat-icon">${icon('compare')}</span><div><strong>${ids.length} servicio${ids.length===1?'':'s'} para comparar</strong><small>Compará precio, duración y alcance. No reserva ni cobra.</small></div><div class="actions">${button('Comparar ('+ids.length+')','compare-open','','')}${button(icon('close'),'compare-clear','','secondary small','aria-label="Vaciar comparación"')}</div></aside>`;}
function comparison(ctx){const offers=ctx.compare.map(id=>ctx.data.offers.find(o=>o.id===id)).filter(Boolean);return `<p class="muted small">Hasta tres propuestas. Cada prestador tiene su tarifa por bloque de 15 minutos.</p><div class="compare-cols" style="--columns:${offers.length}">${offers.map(o=>`<div class="compare-cell"><div>${avatar(person(ctx,o.provider_id))}<h3 style="margin-top:12px">${h(o.title)}</h3><strong>${money(o.price_ars*100)}</strong><p>Por bloque · ${duration(o.blocks)} habitual<br>Total habitual: ${money(o.blocks*o.price_ars*100)}</p><p>${h(o.description)}</p>${link('Ver servicio','/oferta/'+o.id,'secondary small')}</div></div>`).join('')}</div>`;}
function modalWrap(title,content){return `<header class="modal-header"><h2 id="modal-title">${h(title)}</h2><button class="icon-btn" data-action="close-modal" aria-label="Cerrar">${icon('close')}</button></header><div class="modal-content">${content}</div>`;}
function bookForm(ctx,o){const compatible=ctx.data.needs.filter(n=>n.user_id===ctx.user?.id&&n.category===o.category);return `<p class="small muted">Con ${h(o.provider_name)} · ${h(o.title)}. La solicitud no genera un cobro. El prestador debe aceptar antes de que pagues.</p><form data-form="book" data-id="${h(o.id)}"><div class="form-grid"><label class="field">Cantidad de bloques<input name="blocks" type="number" min="1" max="32" step="1" value="${o.blocks}" required></label><label class="field">Fecha y hora local<input name="scheduledAt" type="datetime-local" value="${dateInput(Date.now()+86400000)}" min="${dateInput(Date.now()+5*60000)}" required></label></div>${compatible.length?`<label class="field">Asociar a una necesidad<select name="needId"><option value="">Sin asociar</option>${compatible.map(n=>`<option value="${h(n.id)}">${h(n.title)}</option>`).join('')}</select></label>`:''}<label class="field">Qué necesitás resolver<textarea name="notes" maxlength="1500" placeholder="Contá el alcance. No compartas información sensible."></textarea></label><div id="book-preview">${quoteBox(o.price_ars,o.blocks)}</div><label class="check-label"><input name="quoteAccepted" type="checkbox" required><span>Revisé el alcance, la duración, el precio y las condiciones vigentes. El precio de esta orden no cambia si el prestador modifica luego su publicación.</span></label><div class="form-error" role="alert"></div><button class="btn full" type="submit">Enviar solicitud ${icon('arrow')}</button></form>`;}

return {button,link,avatar,person,photoFor,badge,empty,head,notice,quoteBox,consentBanner,shell,offerCard,categories,home,filteredOffers,explore,offerDetail,publish,accessRequired,orders,orderCard,orderDetail,checkout,seller,needs,profile,finances,agenda,terms,help,authPage,compareTray,comparison,modalWrap,bookForm};})();
__modules['app']=(()=>{
const {api,setCSRF}=__modules['api'];
const {escapeHTML:h,formatARS:money,dateLabel,preview,normal,csv,calendarICS,download,statusLabel,routeFromHash,requestKey}=__modules['format'];
const {icon}=__modules['icons'];
const v=__modules['views'];
const {createMotionController}=__modules['motion'];
const app=document.getElementById('app'),modal=document.getElementById('modal'),modalBody=document.getElementById('modal-body');
const preferences={theme:'light',motion:'auto'};try{Object.assign(preferences,JSON.parse(localStorage.getItem('intera5-appearance')||'{}'));}catch{}
if(!['light','dark'].includes(preferences.theme))preferences.theme='light';if(!['auto','reduced','off'].includes(preferences.motion))preferences.motion='auto';
const state={config:null,legal:null,session:{user:null},user:null,data:{people:[],offers:[],bookings:[],needs:[],favorites:[],categories:[],neighborhoods:[]},route:'/',params:new URLSearchParams(),filters:{q:'',category:'',price:'',neighborhood:'',sort:'',saved:false},orderFilter:'all',compare:[],theme:preferences.theme,calendarYear:new Date().getFullYear(),calendarMonth:new Date().getMonth(),calendarDate:null,order:null,finances:null};
const motion=createMotionController(()=>preferences.motion);let generation=0,modalState=null,lastFocus=null,lastUser=null,writeBusy=false,searchIndex=0;
function appearance(){document.documentElement.dataset.theme=preferences.theme;state.theme=preferences.theme;motion.sync();try{localStorage.setItem('intera5-appearance',JSON.stringify(preferences));}catch{}document.querySelector('.theme-btn')?.replaceChildren();const b=document.querySelector('.theme-btn');if(b)b.innerHTML=icon(preferences.theme==='dark'?'sun':'moon');}
function toast(text,error=false,undo=null){const box=document.getElementById('toasts'),el=document.createElement('div');el.className='toast'+(error?' error':'');const span=document.createElement('span');span.textContent=text;el.append(span);if(undo){const b=document.createElement('button');b.textContent='Deshacer';b.addEventListener('click',async()=>{b.disabled=true;try{await undo();el.remove();}catch(e){showError(e);}});el.append(b);}box.append(el);while(box.children.length>2)box.firstElementChild.remove();setTimeout(()=>el.remove(),undo?12000:6500);}
function showError(e,form=null){if(form){const el=form.querySelector('.form-error');if(el){el.textContent=e.message;motion.feedback('error',el);el.scrollIntoView({block:'nearest',behavior:'auto'});return;}}toast(e.message||'No se pudo completar la operación.',true);}
function closeModal(){if(modal.open)modal.close();modalState=null;}
modal.addEventListener('close',()=>{if(modal.open)return;const prior=lastFocus;lastFocus=null;modalState=null;prior?.isConnected&&prior.focus();});
function openModal(title,content,info={}){if(!modal.open)lastFocus=document.activeElement;modalState=info;modalBody.innerHTML=v.modalWrap(title,content);if(!modal.open)modal.showModal();motion.dialog(modal);setTimeout(()=>{modal.querySelector('input:not([type=checkbox]):not([type=hidden]),textarea,button')?.focus();},50);}
function goto(path){closeModal();if(location.hash==='#'+path)render();else location.hash=path;}
function logged(){if(state.user)return true;goto('/acceso');return false;}
function operable(){if(!logged())return false;if(state.user.suspended){toast('Tu cuenta está restringida. Podés consultar tus datos y registrar un reclamo.',true);return false;}if(!state.user.emailVerified){toast('Verificá tu correo antes de operar.',true);return false;}if(!state.session.conditionsAccepted){termsModal();return false;}return true;}
async function base(){const [config,legal,session,data]=await Promise.all([api('/config'),api('/legal'),api('/session'),api('/bootstrap')]);return {config,legal,session,user:session.user,data};}
function parseRoute(){return routeFromHash(location.hash);}
async function render({scroll=false}={}){const gen=++generation;const routeInfo=parseRoute();try{const b=await base();if(gen!==generation)return;if(lastUser!==b.user?.id){state.compare=[];state.filters.saved=false;lastUser=b.user?.id;}Object.assign(state,b,routeInfo);setCSRF(b.session.csrf);appearance();let content;const r=state.route;const id=decodeURIComponent(r.split('/')[2]||'');
 if(r==='/')content=v.home(state);
 else if(r==='/explorar')content=v.explore(state);
 else if(r==='/necesidades')content=v.needs(state);
 else if(r==='/publicar'){const offer=state.data.offers.find(o=>o.id===state.params.get('edit'));content=offer&&offer.provider_id!==state.user?.id?v.empty('No podés editar esta publicación','Ingresá con la cuenta del prestador.'):v.publish(state,offer);}
 else if(r.startsWith('/oferta/'))content=v.offerDetail(state,state.data.offers.find(o=>o.id===id));
 else if(r==='/ordenes')content=v.orders(state);
 else if(r.startsWith('/orden/')||r.startsWith('/checkout/')){if(!state.user)content=v.accessRequired();else{state.order=await api('/bookings/'+encodeURIComponent(id));content=r.startsWith('/checkout/')?v.checkout(state,state.order):v.orderDetail(state,state.order);}}
 else if(r==='/prestador')content=v.seller(state);
 else if(r==='/movimientos'){if(!state.user)content=v.accessRequired();else{state.finances=await api('/finances');content=v.finances(state,state.finances);}}
 else if(r==='/agenda')content=v.agenda(state);
 else if(r==='/perfil')content=v.profile(state,state.user?state.data.people.find(p=>p.id===state.user.id):null,true);
 else if(r.startsWith('/perfil/'))content=v.profile(state,state.data.people.find(p=>p.id===id),id===state.user?.id);
 else if(r==='/condiciones')content=v.terms(state);
 else if(r==='/ayuda')content=v.help(state);
 else if(r==='/acceso')content=v.authPage(state,'login');
 else if(r==='/registro')content=v.authPage(state,'register');
 else if(r==='/recuperar')content=v.authPage(state,'forgot');
 else if(r.startsWith('/recuperar/'))content=v.authPage(state,'reset');
 else if(r.startsWith('/verificar/'))content=v.authPage(state,'verify');
 else content=v.empty('Esta página no está disponible','Usá la navegación para continuar.',v.link('Volver al inicio','/',''));
 if(gen!==generation)return;app.innerHTML=v.shell(state,content);appearance();motion.refresh(document.getElementById('main'),{transition:scroll});if(scroll)window.scrollTo({top:0,behavior:'instant'});const messages=document.getElementById('messages');if(messages)messages.scrollTop=messages.scrollHeight;
 if(state.params.get('connected')==='1'){toast('La cuenta de Mercado Pago quedó vinculada.');history.replaceState(null,'','#/prestador');}
 }catch(e){if(gen!==generation)return;const err=v.empty('No pudimos cargar esta pantalla.',e.message,v.button('Reintentar','refresh','','')+' '+v.link('Ir al inicio','/','secondary'),'info');app.innerHTML=state.config?v.shell(state,err):`<main class="boot">${err}<p class="small muted">Comprobá que el servidor está en funcionamiento y que abriste su dirección, no el archivo index.html.</p></main>`;}}
async function refreshAfter(message,target=null){closeModal();if(target&&'#'+target!==location.hash){goto(target);if(message)toast(message);return;}await render();if(message)toast(message);}
function termsModal(){openModal('Antes de operar, reglas claras.',`${v.notice('Versión '+h(state.legal.version)+'. La aceptación se registra con la huella del documento.')}<pre class="legal">${h(state.legal.content)}</pre><form data-form="terms"><label class="check-label"><input name="accept" type="checkbox" required><span>Leí y acepto estas condiciones del servicio y privacidad: bloques de 15 minutos con precios $1.000, $2.500 o $5.000 ARS; comisión total INTERA del 5% del bruto, descontada al prestador; cargos de Mercado Pago separados.</span></label><div class="form-error" role="alert"></div><button class="btn full" type="submit">Aceptar y continuar</button></form>`);}
function demoModal(){openModal('Probá con dos personas distintas.',`<p class="small muted">Solo disponible en el simulador local. Cada cambio abre una sesión de prueba en el servidor. No habilita identidades reales ni evita la aceptación de condiciones.</p><div class="demo-people">${state.data.people.filter(p=>p.is_demo).map(p=>`<button class="demo-person" data-action="demo-user" data-id="${h(p.id)}">${v.avatar(p)}<span><strong>${h(p.name)}</strong><small>${h(p.neighborhood)}${p.id==='demo_mateo'?' · prestador sugerido':p.id==='demo_ana'?' · solicitante sugerida':''}</small></span></button>`).join('')}</div>${v.notice('Recorrido sugerido: Mateo → aceptar condiciones → vincular cuenta simulada. Ana → aceptar condiciones → elegir servicio de Mateo → solicitar. Luego Mateo acepta y Ana paga en el simulador.','warning')}`);}
function accountModal(){openModal('Tu espacio en INTERA',`${state.user?`<div class="party">${v.avatar(state.user,'lg')}<div><h3>${h(state.user.name)}</h3><p class="small muted">${h(state.user.email)}</p></div></div>`:''}<div class="settings-options section">${v.link(icon('user')+' Mi perfil','/perfil','secondary')}${v.link(icon('briefcase')+' Soy prestador','/prestador','secondary')}${v.link(icon('wallet')+' Pagos y cobros','/movimientos','secondary')}${v.link(icon('shield')+' Condiciones','/condiciones','secondary')}${v.button('Experiencia visual','settings','','ghost')}${state.config.mode==='mock'?v.button('Cambiar perfil de prueba','demo','','ghost'):''}${state.user?v.button('Cerrar sesión','logout','','danger'):v.link('Ingresar','/acceso','')}</div>`);}
function settingsModal(){openModal('A tu manera.',`<p class="small muted">Estas preferencias se guardan en este navegador. Nunca alteran saldos, pagos o acuerdos.</p><h3>Apariencia</h3><div class="actions">${v.button(icon('sun')+' Claro','set-theme','light','secondary',`aria-pressed="${preferences.theme==='light'}"`)}${v.button(icon('moon')+' Oscuro','set-theme','dark','secondary',`aria-pressed="${preferences.theme==='dark'}"`)}</div><h3 class="section">Movimiento</h3><div class="settings-options">${[['auto','Automático · respeta el sistema'],['reduced','Reducido · menos movimiento'],['off','Desactivado']].map(([p,t])=>v.button(t,'set-motion',p,'secondary',`aria-pressed="${preferences.motion===p}"`)).join('')}</div><p class="small muted section">Sin sonidos, flashes ni animaciones infinitas de atención. El feedback de una operación aparece después de confirmarse con el servidor.</p>`);}
function reportModal(bookingId=null,subjectId=null){if(!logged())return;openModal('Ayuda y reclamos',`${v.notice(state.config.mode==='mock'?'Queda registrado solo en la base local de prueba.':'Queda registrado para el operador. No es atención inmediata.','warning')}<form data-form="report" data-booking="${h(bookingId||'')}" data-subject="${h(subjectId||'')}"><label class="field">Tipo<select name="type">${bookingId?'<option value="DISPUTE">Disputa del servicio</option>':''}<option value="REFUND_REQUEST">Solicitud de devolución / revisión</option><option value="ABUSE">Conducta inapropiada</option><option value="SUPPORT">Consulta general</option></select></label><label class="field">Qué ocurrió<textarea name="body" minlength="10" maxlength="3000" required></textarea></label><div class="form-error" role="alert"></div><button class="btn full" type="submit">Registrar reclamo</button></form>`);}
function searchModal(){openModal('Encontrá tu próximo paso.',`<label class="field">Buscar en INTERA<input id="quick-search" aria-label="Búsqueda rápida" placeholder="Servicios, personas, mis órdenes…" autocomplete="off"></label><div class="search-results" id="quick-results"></div>`);updateSearch('');}
function updateSearch(query){const target=document.getElementById('quick-results');if(!target)return;const q=normal(query);const sections=[['Inicio','/', 'home'],['Explorar servicios','/explorar','search'],['Ofrecer un servicio','/publicar','plus'],['Mis servicios','/ordenes','swap'],['Soy prestador','/prestador','briefcase'],['Agenda','/agenda','calendar'],['Pagos y cobros','/movimientos','wallet'],['Ayuda','/ayuda','help']].map(([label,path,i])=>({label,path,i,detail:'Sección'}));const results=[...sections,...state.data.offers.filter(o=>o.active).map(o=>({label:o.title,path:'/oferta/'+o.id,i:'spark',detail:money(o.price_ars*100)+' / bloque · '+o.provider_name})),...state.data.people.map(p=>({label:p.name,path:'/perfil/'+p.id,i:'user',detail:p.neighborhood})),...state.data.bookings.map(b=>({label:b.title,path:'/orden/'+b.id,i:'swap',detail:'Tu orden · '+statusLabel(b.status)}))].filter(r=>!q||normal(r.label+' '+r.detail).includes(q)).slice(0,12);target.innerHTML=results.map(r=>`<a class="search-result" href="#${h(r.path)}">${icon(r.i)}<div><strong>${h(r.label)}</strong><small>${h(r.detail)}</small></div></a>`).join('')||'<p class="small muted">No hay resultados. Probá con otra palabra.</p>';searchIndex=0;}
function needForm(){if(!operable())return;openModal('Contá qué necesitás.',`<form data-form="need"><label class="field">Título<input name="title" minlength="6" maxlength="100" required placeholder="Busco ayuda para…"></label><label class="field">Descripción<textarea name="description" minlength="10" maxlength="1500" required></textarea></label><label class="field">Categoría<select name="category">${state.data.categories.map(c=>`<option>${h(c)}</option>`).join('')}</select></label><div class="form-grid"><label class="field">Tarifa máxima por bloque<select name="maxPriceArs">${[1000,2500,5000].map(p=>`<option value="${p}">${money(p*100)} / 15 min</option>`).join('')}</select></label><label class="field">Bloques estimados<input name="blocks" type="number" min="1" max="32" step="1" value="2" required></label></div><p class="small muted">Publicar una necesidad no cobra dinero. Cada orden posterior requiere aceptar un precio específico.</p><div class="form-error" role="alert"></div><button class="btn full" type="submit">Publicar necesidad</button></form>`);}
async function action(name,id,el){switch(name){
 case 'close-modal':closeModal();return;
 case 'refresh':await render();return;
 case 'search':searchModal();return;
 case 'theme':preferences.theme=preferences.theme==='dark'?'light':'dark';appearance();return;
 case 'settings':settingsModal();return;
 case 'set-theme':preferences.theme=id;appearance();settingsModal();return;
 case 'set-motion':preferences.motion=id;appearance();settingsModal();return;
 case 'account':if(!logged())return;accountModal();return;
 case 'demo':if(state.config.mode==='mock')demoModal();return;
 case 'demo-user':{if(state.config.mode!=='mock')return;await api('/demo/login',{method:'POST',data:{userId:id}});closeModal();const path=id==='demo_mateo'?'/prestador':'/';history.replaceState(null,'','#'+path);await render({scroll:true});if(!state.session.conditionsAccepted)termsModal();return;}
 case 'terms':if(!logged())return;termsModal();return;
 case 'how':openModal('Un bloque. Quince minutos. Una solución.',`<div class="step-tile"><span class="step-num">1</span><div><h3>Una tarifa, elegida por el prestador.</h3><p>$1.000, $2.500 o $5.000 ARS por bloque. Multiplicás por la cantidad de bloques para conocer el total.</p></div></div><div class="step-tile"><span class="step-num">2</span><div><h3>Una comisión transparente.</h3><p>INTERA descuenta el 5% del bruto al prestador. Los cargos de Mercado Pago y retenciones no están incluidos en ese 5%.</p></div></div><div class="step-tile"><span class="step-num">3</span><div><h3>El cobro no es una billetera.</h3><p>Mercado Pago procesa el pago y distribuye los importes. Finalizar un servicio no mueve ni libera fondos.</p></div></div><div class="section">${v.link('Ver condiciones completas','/condiciones','secondary full')}</div>`);return;
 case 'activity':{if(!logged())return;const rows=state.data.bookings.filter(b=>!['CANCELLED','COMPLETED'].includes(b.status));openModal('Tu actividad pendiente.',rows.length?rows.map(b=>`<a class="list-row" href="#/orden/${h(b.id)}">${icon('swap')}<div class="grow"><strong>${h(b.title)}</strong><small>${h(statusLabel(b.status))} · ${h(statusLabel(b.payment_status))}</small></div>${icon('chevron')}</a>`).join(''):v.empty('Estás al día','Tus próximos servicios aparecerán acá.','','check'));return;}
 case 'category':state.filters.category=id;if(el.dataset.local){await render();}else goto('/explorar');return;
 case 'clear-filters':state.filters={q:'',category:'',price:'',neighborhood:'',sort:'',saved:false};await render();return;
 case 'show-saved':if(!logged())return;state.filters.saved=!state.filters.saved;await render();return;
 case 'favorite':{if(!operable())return;const result=await api('/favorites/'+encodeURIComponent(id),{method:'POST',data:{}});closeModal();await render();motion.feedback('favorite',`[data-action="favorite"][data-id="${CSS.escape(id)}"]`);const owner=state.user.id;toast(result.saved?'Guardaste este servicio.':'Quitaste este servicio de Guardados.',false,async()=>{if(state.user?.id!==owner)throw new Error('Volvé al perfil que guardó este servicio.');const current=await api('/session');if(current.user?.id!==owner)throw new Error('La sesión cambió. Recargá la aplicación.');setCSRF(current.csrf);await api('/favorites/'+encodeURIComponent(id),{method:'POST',data:{}});await render();});return;}
 case 'compare':{if(state.compare.includes(id))state.compare=state.compare.filter(v=>v!==id);else{if(state.compare.length>=3){toast('Podés comparar hasta tres servicios.',true);return;}state.compare.push(id);}closeModal();await render();motion.feedback('compare','.compare-tray');return;}
 case 'compare-open':openModal('Compará antes de elegir.',v.comparison(state));return;
 case 'compare-clear':state.compare=[];await render();return;
 case 'quick':{const o=state.data.offers.find(o=>o.id===id);if(!o)return;openModal(o.title,`<div class="party">${v.avatar(v.person(state,o.provider_id))}<strong>${h(o.provider_name)}</strong></div><p class="small muted section">${h(o.description)}</p>${v.quoteBox(o.price_ars,o.blocks)}<div class="actions">${v.link('Ver servicio completo','/oferta/'+id,'secondary')}${o.provider_id!==state.user?.id&&o.ready?v.button('Acordar este servicio','book',id,''):''}</div>`);return;}
 case 'book':{if(!operable())return;const o=state.data.offers.find(o=>o.id===id);if(!o||!o.ready){toast('El prestador debe aceptar condiciones y vincular su cuenta.',true);return;}openModal('Acordá tu servicio.',v.bookForm(state,o),{kind:'book',offer:o});return;}
 case 'checkout':if(!operable())return;goto('/checkout/'+id);return;
 case 'mp-connect':{if(!operable())return;const result=await api('/mp/connect',{method:'POST',data:{}});if(result.url){location.assign(result.url);return;}await refreshAfter('Cuenta de cobro simulada vinculada. No se mueve dinero real.');return;}
 case 'mp-pause':openModal('Pausar nuevos cobros',`<p>Las publicaciones dejan de aceptar nuevas contrataciones. Las credenciales cifradas se conservan para conciliar pagos y devoluciones anteriores; podés revocar también la autorización desde Mercado Pago.</p><form data-form="pause-mp"><div class="form-error" role="alert"></div><button class="btn danger full" type="submit">Pausar nuevos cobros</button></form>`);return;
 case 'toggle-offer':if(!operable())return;await api('/offers/'+encodeURIComponent(id),{method:'PATCH',data:{active:el.dataset.active==='1'}});await refreshAfter('Publicación actualizada.');return;
 case 'order-filter':state.orderFilter=id;await render();return;
 case 'accept':case 'start':case 'complete':case 'confirm':{if(!operable())return;const labels={accept:'Aceptar servicio',start:'Iniciar servicio',complete:'Marcar servicio realizado',confirm:'Confirmar servicio recibido'};openModal(labels[name],`<p>${name==='accept'?'Al aceptar, el solicitante podrá iniciar un pago. Esperá su aprobación antes de prestar el servicio.':name==='start'?'Iniciá únicamente cuando vayan a comenzar la prestación acordada.':name==='complete'?'Confirmá que completaste tu parte. El solicitante podrá validar o reclamar.':'Confirmá únicamente si recibiste el servicio. Esta acción no transfiere ni libera dinero.'}</p><form data-form="order-action" data-id="${h(id)}" data-next="${name}"><div class="form-error" role="alert"></div><button class="btn full" type="submit">${labels[name]}</button></form>`);return;}
 case 'cancel':{if(!logged())return;openModal('Cancelar este servicio',`<p class="small muted">Antes del inicio se solicita el reembolso de los pagos aprobados. Una solicitud de devolución no equivale a dinero ya devuelto: se verifica con Mercado Pago.</p><form data-form="cancel" data-id="${h(id)}"><label class="field">Motivo<textarea name="reason" minlength="10" maxlength="1500" required></textarea></label><div class="form-error" role="alert"></div><button class="btn danger full" type="submit">Cancelar y solicitar devolución si corresponde</button></form>`);return;}
 case 'extension':{if(!operable())return;const b=state.data.bookings.find(b=>b.id===id);openModal('Proponer tiempo adicional',`<p class="small muted">Cada bloque conserva la tarifa original de ${money(b.price_ars*100)}. Se crea una orden de ampliación, con pago separado y aprobación expresa del solicitante. No se carga automáticamente a su tarjeta.</p><form data-form="extension" data-id="${h(id)}"><label class="field">Bloques extra<input name="blocks" type="number" min="1" max="32" step="1" value="1" required></label><div class="form-error" role="alert"></div><button class="btn full" type="submit">Proponer ampliación</button></form>`);return;}
 case 'review':if(!operable())return;openModal('¿Cómo fue la experiencia?',`<p class="small muted">Las valoraciones permanecen privadas hasta que ambas partes evalúen o transcurran siete días desde la confirmación.</p><form data-form="review" data-id="${h(id)}"><label class="field">Valoración<select name="rating"><option value="5">5 · Excelente</option><option value="4">4 · Muy buena</option><option value="3">3 · Correcta</option><option value="2">2 · Mejorable</option><option value="1">1 · Mala experiencia</option></select></label><label class="field">Comentario opcional<textarea name="comment" maxlength="1000"></textarea></label><div class="form-error" role="alert"></div><button class="btn full" type="submit">Guardar valoración</button></form>`);return;
 case 'report-order':reportModal(id);return;
 case 'report-profile':reportModal(null,id);return;
 case 'block':if(!logged())return;openModal('Cambiar bloqueo de perfil',`<p>Si no está bloqueado, se bloquearán nuevas contrataciones entre ambas cuentas. Si ya está bloqueado, se lo desbloqueará. El historial de servicios y los reclamos siguen disponibles.</p><form data-form="block" data-id="${h(id)}"><div class="form-error" role="alert"></div><button class="btn danger full" type="submit">Confirmar cambio</button></form>`);return;
 case 'reconcile':{if(!logged())return;await api('/bookings/'+encodeURIComponent(id)+'/reconcile',{method:'POST',data:{}});toast('Se solicitó consultar el estado con Mercado Pago. No se acredita por la URL de retorno.');setTimeout(()=>{if(state.route==='/orden/'+id)render();},5500);return;}
 case 'simulate':{if(state.config.mode!=='mock'||!operable())return;await api('/demo/pay/'+encodeURIComponent(id),{method:'POST',data:{status:el.dataset.status}});await refreshAfter('Resultado simulado registrado en el servidor.','/orden/'+id);return;}
 case 'draft':{const ta=document.querySelector('form[data-form="chat"] textarea');if(ta){if(ta.value.trim()){toast('Ya tenés un borrador: no lo reemplazamos.');return;}ta.value=el.dataset.text;ta.dispatchEvent(new Event('input',{bubbles:true}));ta.focus();}return;}
 case 'emoji':{const ta=document.querySelector('form[data-form="chat"] textarea');if(ta&&ta.value.length+(el.dataset.text||'').length<2000){ta.setRangeText(el.dataset.text,ta.selectionStart,ta.selectionEnd,'end');ta.dispatchEvent(new Event('input',{bubbles:true}));ta.focus();}return;}
 case 'need':needForm();return;
 case 'need-close':if(!operable())return;await api('/needs/'+encodeURIComponent(id)+'/close',{method:'POST',data:{}});await refreshAfter('Necesidad cerrada.');return;
 case 'need-match':{const n=state.data.needs.find(n=>n.id===id);if(!n)return;const rows=state.data.offers.filter(o=>o.active&&o.category===n.category&&o.price_ars<=n.max_price_ars&&o.provider_id!==n.user_id);openModal('Servicios que coinciden.',`<p class="small muted">Tarifa por bloque igual o menor a ${money(n.max_price_ars*100)} en ${h(n.category)}. La persona que solicita debe enviar y confirmar su propia orden.</p>${rows.length?rows.map(o=>`<a class="list-row" href="#/oferta/${h(o.id)}">${v.avatar(v.person(state,o.provider_id))}<div class="grow"><strong>${h(o.title)}</strong><small>${money(o.price_ars*100)} / 15 min · ${h(o.provider_name)}</small></div>${icon('chevron')}</a>`).join(''):v.empty('Todavía no hay coincidencias','Podés volver a consultar más adelante.')}`);return;}
 case 'calendar-day':state.calendarDate=id?Number(id):null;await render();return;
 case 'month-prev':case 'month-next':{const d=new Date(state.calendarYear,state.calendarMonth+(name==='month-next'?1:-1),1);state.calendarYear=d.getFullYear();state.calendarMonth=d.getMonth();state.calendarDate=null;await render();return;}
 case 'export-order':case 'export-agenda':{if(!logged())return;const rows=state.data.bookings.filter(b=>!['REQUESTED','CANCELLED'].includes(b.status)).filter(b=>name==='export-order'?b.id===id:new Date(b.scheduled_at).getFullYear()===state.calendarYear&&new Date(b.scheduled_at).getMonth()===state.calendarMonth&&(!state.calendarDate||new Date(b.scheduled_at).getDate()===state.calendarDate));if(!rows.length){toast('No hay acuerdos aceptados para exportar.',true);return;}download('INTERA-agenda.ics',calendarICS(rows,state.config.mode==='mock'),'text/calendar;charset=utf-8');toast('Agenda exportada. No envía invitaciones.');return;}
 case 'export-data':if(!logged())return;download('INTERA-mis-datos.json',JSON.stringify(await api('/export'),null,2),'application/json');toast('Exportación personal creada. Guardala en un lugar privado.');return;
 case 'export-finances':{if(!logged())return;const f=await api('/finances');download('INTERA-operaciones.csv',csv([['ID pago','Orden','Servicio','Estado MP','Bruto ARS','Comisión pactada ARS','Comisión reportada ARS','Cargo MP reportado ARS','Neto reportado ARS','Reembolsado ARS','Disponibilidad MP'],...f.payments.map(p=>[p.id,p.booking_id,p.title,p.status,p.amount_cents/100,p.agreed_commission_cents/100,p.commission_reported_cents===null?'No informado':p.commission_reported_cents/100,p.processor_fee_cents===null?'No informado':p.processor_fee_cents/100,p.net_reported_cents===null?'No informado':p.net_reported_cents/100,p.refunded_cents/100,p.release_status])]),'text/csv;charset=utf-8');toast('CSV exportado. No es una factura.');return;}
 case 'resend':{if(!logged())return;const r=await api('/auth/resend',{method:'POST',data:{}});if(r.verificationLink)openModal('Correo simulado.',`<p>Este enlace solo se muestra en el entorno mock. En sandbox/live se envía por correo transaccional.</p><a class="btn full" href="${h(r.verificationLink)}">Verificar correo</a>`);else toast('Se solicitó el envío de un nuevo enlace de verificación.');return;}
 case 'logout':await api('/auth/logout',{method:'POST',data:{}});setCSRF(null);await refreshAfter('Cerraste sesión.','/');return;
 default:throw new Error('Acción no disponible.');
}}
document.addEventListener('click',async e=>{const link=e.target.closest('a[href^="#"]');if(link){if(link.classList.contains('skip')){e.preventDefault();document.getElementById('main')?.focus();return;}if(modal.open&&link.target!=='_blank')closeModal();}const el=e.target.closest('[data-action]');if(!el||el.disabled)return;e.preventDefault();const wasDisabled=el.disabled;el.disabled=true;try{await action(el.dataset.action,el.dataset.id||'',el);}catch(err){showError(err);}finally{if(el.isConnected)el.disabled=wasDisabled;}});
document.addEventListener('submit',async e=>{const form=e.target.closest('form[data-form]');if(!form)return;e.preventDefault();if(writeBusy&&form.dataset.form!=='filters'){toast('Esperá la confirmación de la acción anterior.');return;}const kind=form.dataset.form,data=Object.fromEntries(new FormData(form)),id=form.dataset.id;const submit=form.querySelector('[type=submit]');if(submit)submit.disabled=true;const area=form.querySelector('.form-error');if(area)area.textContent='';writeBusy=true;
 try{switch(kind){
 case 'filters':Object.assign(state.filters,data);await render();break;
 case 'login':await api('/auth/login',{method:'POST',data});await refreshAfter('Ingresaste a tu cuenta.','/');break;
 case 'register':{const r=await api('/auth/register',{method:'POST',data:{...data,conditions:{accept:data.conditions==='on',version:state.legal.version,hash:state.legal.hash}}});history.replaceState(null,'','#/');await render({scroll:true});if(r.verificationLink)openModal('Verificá tu correo de prueba.',`<p>Este enlace se muestra solo en el simulador. En un ambiente conectado, se envía al correo de la cuenta.</p><a class="btn full" href="${h(r.verificationLink)}">Verificar correo</a>`);else toast(r.notice);break;}
 case 'forgot':{const r=await api('/auth/forgot',{method:'POST',data});if(r.recoveryLink){document.getElementById('auth-notice').innerHTML=v.notice(`<strong>Solo para simulación:</strong> <a class="text-link" href="${h(r.recoveryLink)}">abrir enlace de recuperación</a>`,'warning');}else toast(r.notice);break;}
 case 'reset':await api('/auth/reset',{method:'POST',data:{token:state.route.split('/')[2],password:data.password}});await refreshAfter('Contraseña actualizada. Todas las sesiones anteriores fueron revocadas.','/acceso');break;
 case 'verify':await api('/auth/verify',{method:'POST',data:{token:state.route.split('/')[2]}});await refreshAfter('Correo verificado. Ya podés continuar.','/');break;
 case 'terms':await api('/terms/accept',{method:'POST',data:{accept:data.accept==='on',version:state.legal.version,hash:state.legal.hash}});await refreshAfter('Condiciones aceptadas y registradas.');break;
 case 'profile':if(!operable())break;await api('/profile',{method:'PATCH',data});await refreshAfter('Perfil actualizado.');break;
 case 'publish':{if(!operable())break;const input={...data,priceArs:Number(data.priceArs),blocks:Number(data.blocks),modality:'REMOTE',sellerFeeAccepted:data.sellerFeeAccepted==='on'};await api(id?'/offers/'+encodeURIComponent(id):'/offers',{method:id?'PATCH':'POST',data:input});await refreshAfter(id?'Publicación actualizada. Los acuerdos anteriores conservan su precio.':'Tu servicio está publicado.','/prestador');break;}
 case 'book':{if(!operable())break;const o=state.data.offers.find(o=>o.id===id);if(!form.dataset.requestKey)form.dataset.requestKey=requestKey();const r=await api('/bookings',{method:'POST',key:form.dataset.requestKey,data:{offerId:id,blocks:Number(data.blocks),expectedPriceArs:o.price_ars,scheduledAt:new Date(data.scheduledAt).toISOString(),notes:data.notes,needId:data.needId||undefined,quoteAccepted:data.quoteAccepted==='on',termsHash:state.legal.hash}});await refreshAfter('Solicitud enviada. Falta la aceptación del prestador.','/orden/'+r.id);break;}
 case 'checkout':{if(!operable())break;const r=await api('/bookings/'+encodeURIComponent(id)+'/checkout',{method:'POST',data:{accept:data.accept==='on',termsHash:state.legal.hash}});if(r.simulated){document.getElementById('simulation-slot').innerHTML=`<section class="panel accent section"><h3>Simulador de Mercado Pago</h3><p class="small muted">No ingreses datos de tarjetas. Estos botones generan respuestas ficticias en el servidor local.</p><div class="actions">${v.button('Simular aprobado','simulate',id,'','data-status="approved"')}${v.button('Simular pendiente','simulate',id,'secondary','data-status="pending"')}${v.button('Simular rechazado','simulate',id,'secondary','data-status="rejected"')}</div></section>`;document.getElementById('simulation-slot').scrollIntoView({block:'nearest'});}else location.assign(r.checkoutUrl);break;}
 case 'pause-mp':await api('/mp/pause',{method:'POST',data:{}});await refreshAfter('Nuevos cobros pausados.');break;
 case 'order-action':{if(!operable())break;await api('/bookings/'+encodeURIComponent(id)+'/'+form.dataset.next,{method:'POST',data:{}});const next=form.dataset.next;await refreshAfter(next==='confirm'?'Servicio confirmado. Esta acción no mueve dinero.':'Estado del servicio actualizado.','/orden/'+id);if(next==='confirm')motion.feedback('success','.action-panel');break;}
 case 'cancel':await api('/bookings/'+encodeURIComponent(id)+'/cancel',{method:'POST',data});await refreshAfter('Orden cancelada. Revisá el estado de cualquier devolución.','/orden/'+id);break;
 case 'extension':{if(!operable())break;const r=await api('/bookings/'+encodeURIComponent(id)+'/extension',{method:'POST',data:{blocks:Number(data.blocks)}});await refreshAfter('Ampliación propuesta. Requiere un pago separado del solicitante.','/orden/'+r.id);break;}
 case 'chat':if(!operable())break;await api('/bookings/'+encodeURIComponent(id)+'/messages',{method:'POST',data});form.reset();await render();document.querySelector('form[data-form="chat"] textarea')?.focus();motion.feedback('compare','.message:last-child');break;
 case 'review':if(!operable())break;await api('/bookings/'+encodeURIComponent(id)+'/reviews',{method:'POST',data:{rating:Number(data.rating),comment:data.comment}});await refreshAfter('Valoración guardada con privacidad bilateral.');break;
 case 'report':{if(!logged())break;const r=await api('/reports',{method:'POST',data:{...data,bookingId:form.dataset.booking||undefined,subjectId:form.dataset.subject||undefined}});await refreshAfter('Solicitud registrada: '+r.id.slice(0,8).toUpperCase()+'. Conservá esta referencia.');break;}
 case 'block':{const r=await api('/blocks/'+encodeURIComponent(id),{method:'POST',data:{}});await refreshAfter(r.blocked?'Perfil bloqueado para nuevas contrataciones.':'Perfil desbloqueado.','/explorar');break;}
 case 'need':if(!operable())break;await api('/needs',{method:'POST',data:{...data,maxPriceArs:Number(data.maxPriceArs),blocks:Number(data.blocks)}});await refreshAfter('Necesidad publicada. No se generó un cobro.','/necesidades');break;
 default:throw new Error('Formulario no disponible.');
 }}catch(err){showError(err,form);if(err.code==='TERMS_REQUIRED')toast('La versión de condiciones cambió. Recargá y aceptá el texto vigente.',true);}finally{writeBusy=false;if(submit?.isConnected)submit.disabled=false;}});
document.addEventListener('input',e=>{if(e.target.id==='quick-search'){updateSearch(e.target.value);return;}const form=e.target.closest('form');if(!form)return;const data=Object.fromEntries(new FormData(form));if(form.dataset.form==='publish'){const target=document.getElementById('price-preview'),title=document.getElementById('preview-title');if(target)target.innerHTML=v.quoteBox(Number(data.priceArs),Number(data.blocks),{seller:true});if(title)title.textContent=data.title||'Tu próxima propuesta';}if(form.dataset.form==='book'&&modalState?.offer){const target=document.getElementById('book-preview');if(target)target.innerHTML=v.quoteBox(modalState.offer.price_ars,Number(data.blocks));}if(form.dataset.form==='chat'){const count=document.getElementById('chat-counter');if(count)count.textContent=(data.body?.length||0)+' / 2000';}});
document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();if(state.config)searchModal();}if(modal.open&&document.getElementById('quick-search')&&['ArrowDown','ArrowUp'].includes(e.key)){e.preventDefault();const links=[...document.querySelectorAll('#quick-results a')];if(!links.length)return;if(document.activeElement.id!=='quick-search')searchIndex=(searchIndex+(e.key==='ArrowDown'?1:-1)+links.length)%links.length;links[searchIndex]?.focus();}});
window.addEventListener('hashchange',()=>{closeModal();render({scroll:true});});
// Poll only when not editing. No local payment transition, no background simulation.
setInterval(()=>{if(document.hidden||modal.open||writeBusy||document.activeElement?.matches('input,textarea,select')||!state.route.startsWith('/orden/'))return;render();},10000);
appearance();render({scroll:true});

return {};})();
